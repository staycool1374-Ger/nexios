# AUDIT REPORT 2026-09-25T22-46-25Z
PATCH: audits/pending_patch.diff
FILES: .gitignore, docs/specs/debugd.md, src/kernel/arch/aarch64/timer.cpp, src/kernel/arch/riscv64/timer.cpp, src/kernel/arch/x86_64/hal/timer.cpp, src/kernel/debug/debug_regs.cpp, src/kernel/debug/debug_regs.hpp, src/kernel/memory/vmm.cpp, src/kernel/syscall/syscall.cpp, src/kernel/syscall/syscall.hpp, src/kernel/syscall/syscall_handlers_debug.cpp, src/kernel/task/scheduler.cpp, src/kernel/task/scheduler.hpp, src/kernel/task/task.hpp, src/kernel/test/test_debug_syscall.cpp, src/kernel/test/test_expected_counts.hpp, src/kernel/test/test_registry.cpp

## FINDINGS
- [S1] src/kernel/syscall/syscall_handlers_debug.cpp:118 — detach resumes with RUNNING + unlocked enqueue_ready (concurrency boundary / critical-section interference)
  WHY: enqueue_ready/dequeue_ready carry no internal guard (scheduler.cpp:488/554) so callers must hold scheduler_lock_ (cf. set_priority's IrqGuard+SpinLockGuard, test setup's IrqGuard+add_task), and the runq owns READY tasks only (needs_switch treats RUNNING as on-CPU), so an unlocked RUNNING enqueue corrupts pick accounting and can strand or duplicate the task.
- [S1] src/kernel/syscall/syscall_handlers_debug.cpp:64 — binding table without lock or generation tag (concurrency / §12.5 stale-reference)
  WHY: concurrent attaches can both pass the live/EBUSY checks and mint the same slot, and target_id resolves via find_task with no TCB::generation ABA guard (task.hpp:666-669), so a reaped-then-reused id revalidates a stale handle while resolve-then-use races target teardown across the faultable copies.
- [S2] src/kernel/debug/debug_regs.cpp:179 — riscv64 codec off-by-one, x31 aliased to A7 (correctness / preprocessor-branch semantics)
  WHY: per riscv64 OFF_* (RA=0 .. A7=128/idx16 .. T6=240/idx30, SEPC=248/idx31), frame[i] holds x(i+1), so blob[i]=frame[i] shifts every GPR by one and sources x31 from frame[16]=A7 (the write path then clobbers the live A7/syscall-number slot; the synthetic golden encodes the same shift).
- [S2] src/kernel/syscall/syscall.cpp:133 — §11.6 atomics all-or-nothing violated on has_utrap_frame/debug_parked/debugger_id (data race)
  WHY: these fields are stored in one task's syscall/tick context and loaded from another task's data-call/tick context with plain accesses (syscall.cpp handle/handle_fast stores; scheduler.cpp:4005-4006 park stores; detach/data reads), giving cross-task visibility with no ordering guarantee, while only debug_stop_requested uses __atomic consistently.
- [S2] src/kernel/test/test_debug_syscall.cpp:185 — spinner setup failure path unmaps after delete (memory safety)
  WHY: debug_reap_child terminates/drains/deletes the TCB, so the subsequent VMM::unmap_page_in_pml4(kSpinVa, t->page_table_) dereferences freed memory — kernel-context use-after-free on the error path.
- [S3] scheduler.cpp/timers/syscall.cpp — park hook, tick-PC stores, and U-trap proof compile unguarded in release while the table maps ENOSYS
  WHY: dormant in practice (attach unreachable, request never armed) but literally contradicts the spec §10 "no behavior contract" carve-out; harden with CONFIG_DEBUG guards.
- [S3] debug_regs.cpp/handlers — x86_64 164B blob vs 23-qword codec minimum, EIO returned as raw -5 with no kEio constant
  WHY: internally consistent (handlers stage 34 qwords and truncate) with the syscall-frame rsp=0 degradation documented, but the size mismatch and magic -5 deserve named constants, not silent truncation.
- [S3] .gitignore — anchor fix (debug/ → /debug/) rides along with issue #225
  WHY: hygiene with no safety impact, but out-of-scope for the Phase-1 patch and should have been its own commit.
- [S3] Check 7 — context-retrieval obligation met
  WHY: graphify BFS queries ("debug syscalls ...", "syscall dispatch table ...") are pasted with dispositions (used for SyscallNumber/table/dispatch layout) and the vault search was performed with 0 hits (no debugd notes exist to surface), so there is no absence violation.

## PATCH
audits/rejected_patch.diff was written (24 hunks, git apply-clean against the worktree): it adds a scheduler-locked Scheduler::debugger_resume helper that re-queues as READY, serializes the binding table under a dedicated spinlock with TCB-generation tags and double-checked attach, converts all debug-field accesses to __atomic discipline with pre-delivery wakeup rechecks, fixes the riscv64 codec shift plus its test golden, and reorders the spinner teardown to unmap before reap.

DECISION: REJECTED
