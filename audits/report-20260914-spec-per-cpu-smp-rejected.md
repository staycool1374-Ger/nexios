# AUDIT REPORT 2026-09-14T06-46-10Z
PATCH: (none — PRE-IMPLEMENTATION DESIGN review, no code diff exists)
FILES: docs/specs/per-cpu-smp.md (§3.4 Phase C1 design, new text lines 96-201)

## FINDINGS
- [S1] src/kernel/task/scheduler.cpp:656,777-788,826-889,359-361; src/kernel/sync/queue.cpp:549,580,606,631; src/kernel/ipc/ipc.cpp:70,568,618; src/kernel/task/scheduler.cpp:460-546,1748-1927,2855-2930; src/kernel/task/taskdefs.cpp:204-214 — "queue[C] written only by CPU C" is unenforceable: terminate/remove/unregister/add/move-priority (incl. IPC inheritance + on_tick sporadic:1682)/zombie-drain/reap/cleanup-test/snapshot-restore paths all write the VICTIM's queue from the CALLER's (BSP) CPU under the global lock, while AP reschedule() peeks lock-free; ReadyQueueManager ops are plain multi-word non-atomic (ready_queue_manager.cpp:40-44,69-75).
  WHY: Any BSP write to queue[1] races the AP's IrqGuard-only peek/touch of the same intrusive lists + bitmap, giving torn-metadata reads and silent run-queue corruption; the mailbox rule covers only set_task_ready-originated wakes, not the ~12 other writer families.
- [S1] src/kernel/task/taskdefs.cpp:197-213 vs src/kernel/arch/x86_64/hal/smp.cpp:224-234, src/kernel/kernel.cpp:975,1060 — reboot_from_table() kill loop spares only idle_task_[0] and self, but the AP idle (created in ap_main during bring_up, long before reboot) is neither, so it is terminated, zombied, drained and MemPool-freed while the AP executes on its stack; the next AP tick dereferences a freed/poisoned TCB. Design's "~10 sites" is_idle_task update omits this loop.
  WHY: Deterministic every SMP boot (bring_up precedes reboot_from_table in kernel_main); AP triple-faults/hangs before the first test runs.
- [S1] src/kernel/task/scheduler.cpp:1472-1481,1501-1547,1636-1708,1711-1739,1325-1414 — on_tick() global loops have no affinity skip: sporadic replenishment + move_priority(:1682) operate on queue[1] members from the BSP tick; deadline/accounting/wedge-repair paths mutate AP-affine TCBs (incl. ss objects read concurrently by AP effective_priority()).
  WHY: BSP-tick writes to queue[1]/AP-task state race AP dispatch reads (same mechanism as the S1 above) and AP-affine sporadic state is otherwise serviced nowhere (AP skips sporadic by design), so AP sporadic tasks would also starve.
- [S2] src/kernel/task/scheduler.cpp:487-518 (drain_zombie_list comment: "Holding the lock during cleanup would starve the timer ISR") vs §3.4.2 "cleanup_step() additionally takes scheduler_lock_" — the chosen lock contradicts the documented discipline, stalls BSP on_tick try_lock across AP-side MemPool::free, and fails to exclude drain_zombie_list()/flush_zombies() (still IrqGuard-only, callable from BSP task context and on_tick).
  WHY: Global-lock-across-free reintroduces the exact starvation the code comments forbid and leaves two of three zombie-list writers unsynchronized; a dedicated zombie leaf-lock (or IPI delegation) is required instead.
- [S2] src/kernel/test/test_isolate.cpp:113-226 (off_* layout: no per-CPU/mailbox section), :693-708 (only single-CPU atoms cleared), :990-1078 (RSP-match re-identifies BSP current only) — snapshot captures TaskFields+1×RQ POD but no cpu_ctx[1].current, per-CPU atoms, mailbox rings, or per-CPU queues; mailbox entries are raw TCB pointers with no generation guard (cf. irq_delivery waiter_gen pattern).
  WHY: A test leaving a mailbox pending, or an smp-test failure leaving AP current on a destroyed task, leaks across all later tests (stale drain = UAF dispatch on the AP); g_aps_up/boot_madt/per_cpu[] being un-rewound is correct (boot-stable), but live per-CPU runtime state needs capture-or-quiesce semantics the design does not define.
- [S2] §3.4.2 (4-ring + overflow panic) vs §3.4.6 (sched_affinity "enqueue-to-1 while BSP runs from 0", marked single-CPU safe) — the not-up-CPU rule is undefined, so on 0-AP configs every cross-CPU publish is never drained and never snapshot-cleared; four such wakes panics via the design's own test plan. ISR producers (irq_delivery.cpp:219-241, holding r->lock_ across the publish) additionally violate "mailbox never held with another lock".
  WHY: Fail-closed panic is reachable in legitimate planned flows, not just adversarial ones; require an explicit not-up-CPU rule (direct-enqueue or inline drain), snapshot mailbox clear, and a documented slot→mailbox order (acyclic but currently misstated).
- [S2] src/kernel/arch/x86_64/boot/ap_trampoline.nasm:31-75 (PE/PG only; AP CR0 stays reset 0x60000010: TS=0,EM=0; CR4 PAE-only: OSFXSR=0), src/kernel/arch/x86_64/hal/smp.cpp:224-234 (no AP FPU init), src/kernel/kernel.cpp:1477-1516 (#NM handler assumes BSP; fpu_owner aliases per_cpu[0] via linker) — "AP tasks avoid FPU" rests solely on -mgeneral-regs-only (Makefile:131) with no runtime tripwire.
  WHY: x87 executes silently on the AP (no #NM, no owner update → intra-AP cross-task FPU leakage, undetectable), while SSE/FXSAVE #UD (loud but with no AP-aware handler); a single hand-written x87 asm in an AP-reachable path corrupts numerics silently.
- [S2] src/kernel/core/global_state.cpp:604-614 (scheduler_abort_switch_fixup writes shared TSS.RSP0 for ANY current task unconditionally) — the affinity clamp (set_affinity/add_task/switch paths, all gated on page_table_!=0 at scheduler.cpp:2460,2800) does not cover this AP-side writer.
  WHY: An aborted AP switch rebinds the single shared TSS.RSP0 to an AP kernel-task stack; the next BSP ring-3→0 transition pushes its frame onto the wrong stack (silent memory corruption); gate the fixup to CPU0 (or per-CPU TSS).
- [S2] §3.4.3 (no AP IDT load specified; AP tick starts in ap_main during bring_up while BSP later runs reboot/teardown/snapshot machinery; AP-idle allocator/locking path unspecified) — without lidt the first AP timer IRQ triple-faults (current ap_main explicitly has "no IDT"), and there is no start-gate ordering AP liveness against reboot_from_table/snapshot restore.
  WHY: Two missing boot-contract items (lidt + post-reboot start-gate, or IPI quiesce) plus an unspecified creation lock for the AP idle's all_tasks_/ID-allocator mutations; each is fatal or racy on first SMP boot.
- [S3] Verified PASS with hardening notes — Q4 first-dispatch transfers (validate_switch, scheduler.cpp:2028-2104, never validates current's frame; save_target overwritten; create() builds valid created-order frames, task.cpp:1085/1254/1292/1480; empty-queue self-switch no-ops at :2129; REQUIRE AP idle be built via create()); Q5 affinity equivalence holds on single-CPU (default 0x1 + lowest-bit + idle_[0] fallback; fork affinity inheritance unspecified); Q10 cpu_index fallback safe (GS set at kernel.cpp:541 before IDT:595/timer:970/bring_up:975; trampoline sets GS at nasm:67-71; UNIFY with per_cpu_current() hardcoded to [0], percpu.hpp:76-80); APIC 0xEC free (timer 64, spurious 0xFF, 256 stubs); timer_init AP path idempotent (apic.cpp:246-282); iopb_switch_to early-returns for kernel tasks (iopb.cpp:125-126); H2 atomic index still tears entries (debug-only, document own-CPU generation source).
  WHY: No correctness defect in these items as specified; notes are implementation constraints the plan must pin down.

## PATCH
No machine-applicable patch: pre-implementation design review with no code diff. Required design changes for resubmission: (1) replace the unenforceable queue[C] rule with a complete remote-writer policy — route or forbid BSP writes to queue[1..] (terminate/remove/add/move-priority/zombie/reap/cleanup/snapshot/boot paths) or lock AP peeks, and add affinity skips to all on_tick loops; (2) spare ALL idles in reboot_from_table kill loop, cleanup_test_tasks, reap_orphans (exact site list, not "~10"); (3) replace cleanup_step's scheduler_lock_ with a dedicated zombie leaf-lock covering all three drain paths; (4) define snapshot semantics for per-CPU current/atoms/mailbox/queues (capture or AP-quiesce + generation-guarded mailbox entries + mailbox clear); (5) define the not-up-CPU mailbox rule and correct the leaf-lock claim; (6) add AP FPU tripwire (CR0.TS=1 in ap_main + AP-gated #NM fail-stop); (7) CPU0-gate scheduler_abort_switch_fixup; (8) specify AP lidt + post-reboot scheduler start-gate + AP-idle creation path/locking.

DECISION: REJECTED
