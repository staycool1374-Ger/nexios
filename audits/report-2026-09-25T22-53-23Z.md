# AUDIT REPORT 2026-09-25T22-53-23Z
PATCH: audits/pending_patch.diff
FILES: .gitignore, docs/specs/debugd.md, src/kernel/arch/aarch64/timer.cpp, src/kernel/arch/riscv64/timer.cpp, src/kernel/arch/x86_64/hal/timer.cpp, src/kernel/debug/debug_regs.cpp, src/kernel/debug/debug_regs.hpp, src/kernel/memory/vmm.cpp, src/kernel/syscall/syscall.cpp, src/kernel/syscall/syscall.hpp, src/kernel/syscall/syscall_handlers_debug.cpp, src/kernel/task/scheduler.cpp, src/kernel/task/scheduler.hpp, src/kernel/task/task.hpp, src/kernel/test/test_debug_syscall.cpp, src/kernel/test/test_expected_counts.hpp, src/kernel/test/test_registry.cpp

## FINDINGS
- [S3] src/kernel/syscall/syscall_handlers_debug.cpp detach — no slot reclamation when the target is already dead (resolve ESRCH returns before clear)
  WHY: a target that exits while attached leaks its binding slot (64-slot table, no sweeper), ending in eventual EBUSY denial for a debugger that never detaches dead targets.
- [S3] scheduler.cpp/timers/syscall.cpp — park hook, tick-PC stores, and U-trap proof still compile unguarded in release while the table maps ENOSYS (carried from iter-1)
  WHY: dormant in practice (attach unreachable, request never armed) but literally broader than the spec S10 "no behavior contract" carve-out; harden with CONFIG_DEBUG guards in a follow-up.
- [S3] syscall_handlers_debug.cpp/debug_regs.cpp — x86_64 164B blob vs 23-qword codec minimum, EIO returned as raw -5 with no kEio constant (carried from iter-1)
  WHY: internally consistent (handlers stage 34 qwords and truncate; syscall-frame rsp=0 degradation documented) but the size mismatch and magic -5 deserve named constants, not silent truncation.
- [S3] .gitignore — anchor fix (debug/ → /debug/) still rides along with issue #225 (carried from iter-1)
  WHY: hygiene with no safety impact, but out-of-scope for the Phase-1 patch and should have been its own commit.
- [S3] Check 7 — context-retrieval obligation met (carried from iter-1)
  WHY: graphify BFS queries (116-node + 140-node, pasted with dispositions) and the vault search (0 hits, no debugd notes exist) are on the issue thread, so there is no absence violation.

## VERIFIED FIXES (iter-1 S1/S2 closure, no PATCH section — APPROVED)
- Ex-S1 locked READY resume: Scheduler::debugger_resume takes IrqGuard + scheduler_lock_, re-queues as READY only when BLOCKED + debugger-owned + parked; detach now calls it BEFORE clearing slot->live/debugger_id so the id==0 guard no longer strands the target (test-evidence-backed: FAIL then 5/5 PASS plus neighbour re-green).
- Ex-S1 serialized generation-tagged bindings: g_bind_lock serializes mint/clear/resolve, handle=(gen<<32)|idx with gen-0 sentinel, target_gen ABA guard, double-checked EBUSY on attach; Phase-1 mint path is parent-only so no concurrent reaper exists (single-threaded TCBs, kernel never reaps spontaneously).
- Ex-S2 riscv64 codec: read blob[i]=frame[i-1] (x0==0, A7 untouched) and write frame[i]=blob[i+1] match the OFF_* layout, and the golden asserts the CORRECT shift (frame[9..12]→blob[10..13], sepc frame[31]→blob[32]) — not a masked bug.
- Ex-S2 atomic discipline: has_utrap_frame/debug_stop_requested/debug_parked/debugger_id all __atomic with acquire/release pairing plus pre-delivery RUNNING/READY rechecks; lock order bind→scheduler is acyclic (tick park never takes g_bind_lock).
- Ex-S2 spinner teardown: unmap/free now precede reap on all failure paths; VMM tlb_flush addition is ownership-neutral; no heap allocation in ISR/tick paths (static table, bounded 272B/256B stack staging, balanced PMM alloc/free).

DECISION: APPROVED
