# AUDIT REPORT 2026-09-23T195751Z
PATCH: audits/pending_patch.diff
FILES: src/kernel/arch/riscv64/hal/page_table_impl.hpp, src/kernel/arch/riscv64/syscall_entry.S, src/kernel/arch/riscv64/test_riscv64.cpp, src/kernel/memory/vmm.cpp, src/kernel/memory/vmm.hpp, src/kernel/test/test_expected_counts.hpp

## FINDINGS
- [S1] src/kernel/arch/riscv64/syscall_entry.S:77 — user x2 destroyed before OFF_SP save (critical-section / privilege-boundary violation)
  WHY: `la sp` expands to auipc+addi writing x2, so the user sp is lost before `sd x2, OFF_SP(sp)` records the kstack frame instead, and E2 then srets to U-mode with a kernel-stack sp while the M1 stub never touches sp so the test cannot catch it.
- [S3] src/kernel/arch/riscv64/test_riscv64.cpp:612 — stale stub-size comment ("32-byte raw stub" vs the 40-byte array)
  WHY: the comment disagrees with the 10-instruction probe encoding and misleads future stub maintenance.

## PATCH
`audits/rejected_patch.diff` was written and routes the kstack-top load through dead t0 so user x2 survives until its OFF_SP save.

DECISION: REJECTED
