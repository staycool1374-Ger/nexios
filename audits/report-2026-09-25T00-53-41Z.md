# AUDIT REPORT 2026-09-25T00-53-41Z
PATCH: audits/pending_patch.diff
FILES: src/kernel/arch/riscv64/syscall_entry.S, src/kernel/core/global_state.cpp, src/kernel/memory/vmm.cpp, src/kernel/syscall/syscall_handlers_misc.cpp, src/kernel/syscall/syscall_handlers_process.cpp, src/kernel/task/scheduler.cpp, src/kernel/task/scheduler.hpp

## FINDINGS
- [S2] src/kernel/task/scheduler.cpp:614 — cancel_pending_switch_cpu clears every arm atom but not scheduler_force_apply[cpu], so the new dequeue_ready invalidation (via invalidate_pending_switch_to) and quiesce_enter leave a stale one-shot force flag behind a cancelled arm
  WHY: the riscv64 epilogue consumes force unconditionally before the depth/source gate (syscall_entry.S:228-232), so a stale flag force-applies a later unrelated arm at a nesting level the gate would have skipped.
- [S2] src/kernel/task/scheduler.cpp:4010 — rate_monotonic_schedule CLR path clears save/load/next_task_id atoms without clearing force_apply, so a terminate-era force survives onto the next freshly published normal arm
  WHY: switch_to_task publish (scheduler.cpp:3925-3930) never resets force, leaving the one-shot flag armed across arm lifetimes and defeating its terminate-only intent.
- [S3] src/kernel/memory/vmm.cpp:777 — L1 alloc-failure path returns 0 without freeing the already-allocated dst root (phys)
  WHY: the caller only sees 0 and cannot recover phys, so one PMM page leaks on this OOM path (unwind at vmm.cpp:823 frees L1/L2 but has the same dst-root omission).
- [S3] src/kernel/task/scheduler.cpp:4121 — switch_away_from_terminating sets force_apply on all arches but only the riscv64 epilogue consumes it (no reader in arch/x86_64 or arch/aarch64)
  WHY: on x86_64/aarch64 the flag is write-only litter that outlives its arm, while terminate arms there get no force protection the patch narrative implies.
- [S3] src/kernel/task/scheduler.cpp:3120 — reap_orphans now defers every parented zombie of a live parent (can_reap = terminated), dropping the not-waiting fast path
  WHY: a live parent that never calls waitpid retains each exited child until parent exit, accumulating TCB/stack/page-table pages against the MAX_TASKS cap.

## PATCH
audits/rejected_patch.diff was written to clear force_apply on every arm-cancel path and free the dst root on both clone failure paths.

DECISION: REJECTED
