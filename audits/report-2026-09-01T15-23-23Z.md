# AUDIT REPORT 2026-09-01T15-23-23Z
PATCH: audits/pending_patch.diff
FILES: docs/specs/cspace.md, src/kernel/arch/hal/iopb.hpp, src/kernel/arch/x86_64/hal/iopb.cpp, src/kernel/cap/mmio.cpp, src/kernel/cap/mmio.hpp, src/kernel/memory/vmm.cpp, src/kernel/nexios_config.h, src/kernel/syscall/syscall.hpp, src/kernel/syscall/syscall_handlers_mmio.cpp, src/kernel/task/task.cpp, src/kernel/test/test_cap_mmio.cpp, src/kernel/test/test_cap_mmio_user.cpp, src/kernel/test/test_expected_counts.hpp, src/kernel/test/test_isolate.cpp, src/kernel/test/test_registry.cpp

## FINDINGS
- [S1] src/kernel/cap/mmio.cpp:347 (MmioUserMap::map) / src/kernel/elf/elf.cpp:716-717,775-778 (exec_into_current, NOT in patch) — use-after-free write via a stored PML4 that dangles across exec.
  WHY: `MmioUserMap::Slot::pml4` captures `task.page_table_` at map time, but `exec_into_current` swaps the live task's PML4 to a fresh clone and frees the OLD one (`VMM::free_user_pages(old_pml4); PMM::free_page(old_pml4);`). The registry is never drained on exec, so a task that maps MMIO then execs leaves a slot whose `pml4` points at freed page-table memory; the next `sys_mmio_unmap` (`unmap`), cap `revoke`/`dispose` (`invalidate_cap`), or `cleanup()` (`drain_task`) calls `VMM::unmap_mmio_from_cap(mmio, va, stored_freed_pml4)` → `map_page_in_pml4` writes PTE entries into freed pages — a use-after-free write into memory that may be reallocated to any subsystem (memory corruption). `BufferPool::unmap_all(*tcb)` is drained at elf.cpp:714 before the swap precisely to avoid this class of dangling-PTE hazard; the MMIO registry is missing the equivalent drain. `fork`/clone is safe (child has a fresh task id, no slots), and the test fixtures never exec, so this is unreachable in tests but reachable from user space via the new `SYS_MMIO_MAP` + `SYS_EXEC` + `SYS_MMIO_UNMAP`/terminate sequence. Critical check #4 (memory safety / double-free / UAF) FAILS.
  FIX: `audits/rejected_patch.diff` adds `cap::MmioUserMap::drain_task(*tcb);` in `exec_into_current` immediately after `BufferPool::unmap_all(*tcb)` (elf.cpp:714) and before the old PML4 is freed, plus the `#include <kernel/cap/mmio.hpp>`.
- [S3] src/kernel/syscall/syscall_handlers_mmio.cpp:83-92 — ledger-add-before-grant leaves a phantom record if `iopb_grant_range` failed after `iopb_ledger_add` succeeded (unreachable: a successful `iopb_claim` immediately precedes with no intervening slot release).  Confirmed still acceptable/documented.
- [S3] src/kernel/memory/vmm.cpp (4 sites) — ENSURE relaxation `if (user && phys_addr < PMM::total_memory())` permits mapping any phys >= total_memory() user-accessible without `is_user_page`; contained — only reachable via capability-gated `VMM::map_mmio_from_cap` (rejects revoked/IO/zero caps).  Confirmed still acceptable/documented.
- [S3] src/kernel/test/test_cap_mmio.cpp (ioport_ledger_dropped_at_cleanup) — `iopb_grant_count(*fresh) == 0` is owner-keyed and would not catch a missing `iopb_ledger_drop_task` (the stale entry belongs to the old TCB pointer, which never matches `fresh`); the UAF that motivated iter-1 is fixed, but the regression would not fire on a dropped call. Test-quality note only.
- [S3] src/kernel/test/test_expected_counts.hpp:27 — `all` = 1025 appears 10 too high (main registered 1003 + cap_mmio(+4) + cap_mmio_user(+8) = 1015); `validate_class_count` only warns on mismatch, so it does not break the gate, but the table should read 1015.

## PATCH
`audits/rejected_patch.diff` was written (applies cleanly, verified with `git apply --check`): it drains the task's user MMIO mappings in `exec_into_current` before the old PML4 is freed, closing the S1 use-after-free on the stored `pml4` across exec.

DECISION: REJECTED
