# AUDIT REPORT 2026-08-30T20-40-06Z
PATCH: audits/pending_patch.diff
FILES: prompts/STATE.md, src/kernel/arch/aarch64/boot.S, src/kernel/arch/aarch64/hal/gic.hpp, src/kernel/arch/aarch64/interrupt_controller.cpp, src/kernel/arch/aarch64/syscall_entry.S, src/kernel/arch/aarch64/timer.cpp, src/kernel/arch/aarch64/vectors.S, src/kernel/arch/hal/timer.hpp, src/kernel/elf/elf.cpp, src/kernel/irq_delivery.cpp, src/kernel/kernel.cpp, src/kernel/memory/pmm.cpp, src/kernel/memory/pmm.hpp, src/kernel/memory/vmm.cpp, src/kernel/memory/vmm.hpp, src/kernel/syscall/syscall_handlers_iommu.cpp, src/kernel/task/scheduler.cpp, src/kernel/task/task.cpp, src/kernel/test/test_cap_irq.cpp, src/kernel/test/test_cap_mmio.cpp, src/kernel/test/test_expected_counts.hpp, src/kernel/test/test_pmm.cpp, src/lib/constants.hpp, test-history.txt

## FINDINGS

- [S1] src/kernel/memory/vmm.cpp:328 (map_page aarch64 2MiB-huge-split, lines 305-335) — iteration-1 S1 fix INCOMPLETE: third leaf-emission site still emits invalid L3 descriptors.
  WHY: The aarch64 branch of the `map_page` huge-split loop emits `new_pt[i] = (huge_base + i*0x1000) | base_flags` with `base_flags = pd[pd_idx] & (PAGE_PRESENT|PAGE_AF|PAGE_UXN|PAGE_PXN)` — no `PAGE_TABLE` (parent block descriptor has bits[1:0]=01 → split leaves inherit bits[1:0]=01, a block-at-invalid-level encoding that faults on every access to the split 2MiB region) and no `PAGE_ATTR_NORMAL` in the mask (leaves would also select AttrIndx=0 = Device, unlike the two fixed sites at vmm.cpp:158 and vmm.cpp:574 which OR `PAGE_TABLE | PAGE_ATTR_NORMAL`). Directly contradicts this iteration's verification claim "no site still emits bit1=0 leaves". Reachable on aarch64 by in-tree `VMM::map_page` callers that target block-mapped VAs (e.g. virtio_pci.cpp:53 and dma.cpp:50 map HHDM/identity VAs; boot.S maps the HHDM window and MMIO as 2MiB blocks) — not exercised by the current boot path, but any driver activation corrupts the whole 2MiB region. Additionally vmm.cpp:331 sets PAGE_AF (bit 10) on the new TABLE descriptor, where bit 10 is RES0 (meaningless; the two fixed sites write tables without it).

- [S3] src/kernel/arch/aarch64/vectors.S:106-107 — el0_sync fault path erets with caller-saved user registers destroyed.
  WHY: `bl aarch64_el0_fault_handler` clobbers x0-x18/x30 and the path erets without a register restore; safe today only because the handler unconditionally parks (dequeue_ready + BLOCKED) the faulting task, which is never re-dispatched — if parking ever becomes conditional, this eret resumes user code with garbage state. Design note to anchor in the H2 port follow-up.

## VERIFICATION OF ITERATION-1 FINDINGS (all re-checked)
- vmm.cpp split sites: get_table (vmm.cpp:154-159) and map_page_in_pml4 (vmm.cpp:569-575) now OR `PAGE_TABLE | PAGE_ATTR_NORMAL` → bits[1:0]=11 leaves ✓; leaf sites map_page:350 and map_page_in_pml4:599 ✓; PAGE_AF corrected to bit 10 (vmm.hpp) matching MAIR 0xFF00 (boot.S:197: Attr0=Device, Attr1=Normal WB) ✓ — but the third site above was missed (S1 above).
- vectors.S el0_sync/el0_irq stash-carry: verified correct on BOTH paths — stash→carry(x1=&stash)→switch→reload→(sync: re-stash on task stack, popped before syscall_entry; irq: regs→save_all); no-switch path pops in place; the abandoned old-stack stash on the switch path writes only [SP_EL1_rest-16, SP_EL1_rest) on the task's OWN kernel stack — popped-free space below the restore point that is overwritten before any read (288-byte frame algebra checks out for save_all/restore_all/syscall_entry and the 288-byte initial frames from task.cpp/elf.cpp) ✓. No 16-byte stash corrupts live stack data on any dispatch path ✓.
- syscall_entry.S: `str x0,[sp,#0]` (slot-0 ret publish) precedes the arm check ✓; no-switch epilogue still reads x0 from +272 ✓; switch path saves the frame with slot0=ret, resume via restore_all loads x0@[sp,#0] ✓; IRQ (el1_irq) consuming the arm mid-epilogue is consistent on resume (arm cleared → no-switch epilogue → ret from +272) ✓; save-area layout (x0@0…x30@240, sp_el0@248, elr@256, spsr@264) matches between syscall_entry.S and vectors.S restore path ✓.
- test_pmm.cpp: `want_end = win_base + min(window_pages, total_pages - win_base)` matches compute_window_pages clamping for both arches; no underflow on live configs (win_end>win_base asserted first) ✓; doc-block matches (minor duplicate clause only).
- aarch64_el0_fault_handler: valid-bit (`&1`) checks at L0/L1/L2 of the PT walk ✓; fault_reported[] single-writer safety documented (exception context, DAIF masked on entry, single core) ✓; IrqGuard is proper state-restoring RAII (irq_guard.hpp:37-45) ✓; dequeue_ready on current task matches pre-existing usage (scheduler.cpp:1935, 3082) and TaskQueue::remove is link-walk tolerant ✓; H2 limitation documented in vectors.S NOTE ✓; merged signature/first-statement line split in scheduler.cpp ✓ (x86 `f_rflags` now correctly scoped inside the x86 branch).
- Critical checks: no dynamic allocation in any patched critical path (fault handler uses a static array + polling debug_write; PMM init/boot paths static) ✓; PMM window statics written only under pmm_lock_ in init/init_err, read under the same regime as the previous constants ✓; no spinlock held across reschedule; INV-2 not implicated ✓; arch test-gates are by-architecture only (no CONFIG_DEBUG asymmetry; expected-counts table is per-arch and aarch64 entries are 0=unchecked, so the x86-only test exclusions are consistent) ✓; memory safety: free_page/free_page_err reject below-window indices consistently (same convention as out-of-span), pool placement guarded (window_pages > pool+16, pool_start > reserved_end_page), multi-page scan loop rewritten underflow-safe ✓; tlbi vmalle1 sequence correctly ordered (TTBR0 write → dsb → tlbi → dsb → clear global → isb) ✓; GIC IGROUPR=0/CTLR enable + intid>=1020 guard EOIs all special values without dispatch (no IRQ storm) ✓; timer re-arm uses stored tick_interval_ with 0→1 guard ✓; x86_64 window math is byte-identical (RAM_BASE_FALLBACK=0) ✓.

## PATCH
DECISION: REJECTED. Corrective patch for the S1 written to audits/rejected_patch.diff (overwrites the iteration-1 file, per protocol): apply with `git apply audits/rejected_patch.diff` from the repo root. It completes the third aarch64 split site (vmm.cpp:322 mask += PAGE_ATTR_NORMAL; loop ORs PAGE_TABLE | PAGE_ATTR_NORMAL with the standard comment) and drops the RES0 AF bit from the new table descriptor. After applying: rebuild, re-run memory_pmm + aarch64 boot smoke, and re-submit the cumulative diff as iteration 3.

DECISION: REJECTED
