# AUDIT REPORT 2026-09-26T10-28-42Z
PATCH: audits/pending_patch.diff
FILES: Makefile, docs/specs/debugd.md, src/kernel/arch/aarch64/interrupt_controller.cpp, src/kernel/arch/riscv64/interrupt_controller.cpp, src/kernel/arch/x86_64/hal/idt.cpp, src/kernel/debug/debug_bind.hpp, src/kernel/debug/debug_regs.cpp, src/kernel/debug/debug_regs.hpp, src/kernel/debug/debug_stop.cpp, src/kernel/debug/debug_stop.hpp, src/kernel/kernel.cpp, src/kernel/nexios_config.h, src/kernel/syscall/syscall.hpp, src/kernel/syscall/syscall_handlers_debug.cpp, src/kernel/task/scheduler.cpp, src/kernel/task/scheduler.hpp, src/kernel/task/task.cpp, src/kernel/task/task.hpp, src/kernel/test/test_debug_syscall.cpp, src/kernel/test/test_expected_counts.hpp, src/kernel/test/test_isolate.cpp

## FINDINGS
- [S2] src/kernel/debug/debug_stop.cpp:235 (debug_bp_consume_temp) — consumed temp breakpoint's original bytes are never restored into the target
  WHY: The slot is freed under g_bp_lock but no path writes back g_bp[i].orig, so the emulated-step trap insn (ebreak/c.ebreak/brk) stays patched at the new pc and re-traps spuriously on the next resume (riscv64 + aarch64 only; x86 TF path unaffected).
- [S3] src/kernel/debug/debug_stop.cpp (emu_step_plant) — temp length sized from the current-pc insn, not the next-VA insn (carried from prior report, still present in new tree)
  WHY: When the two lengths differ the temp overwrites one extra halfword, but the snapshot/restore is byte-exact after the S2 fix and page-edge failure stays fail-closed (parked).
- [S3] src/kernel/debug/debug_stop.cpp (debug_route_fault comment) — comment claims "nested scheduler_lock_ -> g_stop_lock_" but the code is sequential (debug_park_stop releases before stop_enqueue)
  WHY: Comment-only inaccuracy; the sequential order is the safer discipline and matches the DeathNotify precedent, but the comment misdescribes the actual nesting.
- [S3] src/kernel/debug/debug_stop.cpp (debug_route_fault temp-hit conversion) — STEP event for a consumed temp carries the breakpoint trap's fault_num (riscv scause 3 / aarch64 EC 0x3C), not a step identifier
  WHY: Observability only; the kind field correctly reports STEP and no consumer branches on fault_num for STEP events, but a GDB stub will read a breakpoint trap number on a step stop.
- [S3] src/kernel/debug/debug_stop.cpp (debug_poll_event/debug_dropped_count/debug_drop_target/debug_snapshot_reset/debug_step_complete) — no CONFIG_DEBUG release stubs while the fault/bp/continue/step/publish paths are all carved out
  WHY: Harmless in practice (static-table ops on empty tables, unreachable via the ENOSYS dispatch, no locks taken across contexts) but the same release-symmetry gap as the prior carried S3.
- [S3] process note — check-7 retrieval substance is on #226 but the 140-node syscall query disposition and the planner plan are referenced ("on file") rather than pasted
  WHY: Two comments carry a 128-node graphify query with used-disposition plus vault 0-hit searches and planner delegation, so there is no absence violation; the second query and the plan text itself are not inspectable on the thread.
- [S3] process note — aarch64 gates red at audit time (9/12 per #226 10:14Z; fault-routing/overflow/poll-empty isolated to #235, x86_64+riscv64 12/12 green, no assertion masking found)
  WHY: No arch-specific safety defect found beyond the S2 above (which affects both emulation arches and is masked in tests because they detach without continuing after STEP); the residue points at resume-frame/QEMU behavior outside these lines, but VERIFIED cannot be claimed on aarch64 until #235 closes — merge scoping is the owner's call.

## VERIFIED FIXES (prior Phase-2 S3 dispositions, confirmed in new tree)
- Rearm staleness FIXED: Scheduler::debug_park_stop clears debug_rearm_va on every non-STEP park (kind != 2, scheduler.cpp), so a later unrelated STEP completion can no longer re-insert an unasked breakpoint.
- Exit-code fidelity DOCUMENTED: detach sel0 and debug_drain_debugger terminate fault-stopped targets with the latched tgt->exit_code and the comments state the router synthesizes no signals (GDB detach semantics: only genuine FAULT stops terminate).
- RISC-V stval-vs-sepc FIXED: the router reports stval when nonzero else sepc for FAULT stops (mirroring x86 rip/aarch64 ELR for non-address faults), scause 3 reports BREAKPOINT at sepc.
- AArch64 stepping DEVIATES cleanly: emu_step_plant covers riscv64+aarch64 (RVC decode vs fixed-4B, SIE masking riscv-only); no SS/MDSCR use anywhere outside reserved-for-future comments; spec §4 documents the deviation.
- Lock order holds everywhere: poll snapshots ownership via debug_collect_owned (bind released on return) then scans under the queue lock only; sel 3-6 nest bind -> bp; detach/drain nest bind -> bp/scheduler and never stop -> bind; no TOCTOU wakeup loss (enqueue and scan serialize on g_stop_lock, debugger drains to EAGAIN per Notify pulse).
- Exact-phys walk contract holds: bp_write_at/bp_read_at, read_mem/write_mem, and all test helpers use HHDM_OFFSET + phys directly with never-add-page_off comments; kBpInsn selection honors RVC per-VA; IDT #BP gate is DPL3 0xEE like SYSCALL; kUserYieldStubVa has a single canonical definition; release stubs carry [[maybe_unused]]; no heap allocation on any router/poll/park path.

## PATCH
`audits/rejected_patch.diff` was written and applies cleanly with `git apply --check`: snapshot the temp shadow's VA/orig-bytes/len under g_bp_lock in debug_bp_consume_temp and restore them via bp_write_at outside the lock at the consume site in debug_route_fault.

DECISION: REJECTED
