# AUDIT REPORT 2026-09-19T12-30-55Z
PATCH: audits/pending_patch.diff
FILES: Makefile, mk/rules.mk, tools/build-picolibc.sh, userspace/picolibc/hello.c, userspace/picolibc/nexios_glue.c

## FINDINGS
- [S3] userspace/picolibc/nexios_glue.c:44-50 — int-narrowing cast (unsigned long count → int) truncates counts > INT_MAX instead of fail-closed EINVAL
  WHY: inherits the #71 `_`-stub `int count` interface so behavior matches the in-tree path, but a >2GB write silently shortens rather than erroring.
- [S3] userspace/picolibc/nexios_glue.c:60-62 — open() drops mode (passes 0), O_CREAT creates mode-blind
  WHY: matches kernel OPEN (path+flags only, `_open` `(void)mode` precedent), so no new defect, but hosted programs cannot set create-mode until the kernel ABI grows a mode arg.
- [S3] userspace/picolibc/nexios_glue.c:33-42 — sbrk() duplicates stdlib.c logic with no drift anchor
  WHY: duplication is justified (pulling stdlib.o would drag malloc/free into collision with picolibc's) and byte-identical to HEAD stdlib.c:42-49, but a future stdlib.c fix will silently diverge from the glue copy.
- [S3] mk/rules.mk:236-237 — comment cites "first-match-wins" for static-pattern precedence
  WHY: terminology is imprecise (static-pattern targets win over implicit rules by class, not by order), but the mechanism itself is deterministic and correct.

DECISION: APPROVED
