# AUDIT REPORT 2026-09-25T15-05-07Z
PATCH: audits/pending_patch.diff
FILES: .github/workflows/ci.yml, Makefile, tools/debugd/include/gdb_rsp.hpp, tools/debugd/include/target_controller.hpp, tools/debugd/tests/mock_transport.hpp, tools/debugd/tests/test_main.cpp

## FINDINGS
- [S3] tools/debugd/tests/test_main.cpp:290 — wrong golden checksum: `$T05#b8` should be `$T05#b9` (0x54+0x30+0x35=185=0xB9, independently recomputed).
  WHY: The string is only round-tripped through write_all and never fed to RspParser, so the gate does not false-pass, but the fixture teaches an invalid wire frame to the Phase-4 oracle.
- [S3] tools/debugd/include/gdb_rsp.hpp:219 — `G<hex>` (kWriteRegs) parse branch has zero test coverage although §11 acceptance explicitly lists `g/G`.
  WHY: The 4-line branch is correct by inspection, but the claimed acceptance coverage is incomplete for one frozen-subset verb (no positive `G<hex>` or empty-reject case among the 23 tests).
- [S3] tools/debugd/tests/test_main.cpp:160 — `H` single-thread model tests only the positive `Hg0`/`Hc-1` cases; rejection (`E01`) of all other thread selectors is untested and lives nowhere in this patch.
  WHY: ParseCommand accepts any `H<op><tid>` syntactically, so the §5 "everything else E01" rule must be enforced in Phase-4 dispatch, which should record that obligation.
- [S3] tools/debugd/include/gdb_rsp.hpp:197 — ParseHexUint silently wraps on >16 hex digits with no overflow rejection.
  WHY: Overlong `m/M/Z` address fields wrap mod 2^64 instead of failing like other garbage, operator-input-only and debug-build-scoped but stricter than the documented reject-garbage discipline.
- [S3] tools/debugd/include/gdb_rsp.hpp:161 — oversize BadChecksum keeps state in kData while checksum-mismatch BadChecksum drops to Idle: two post-states for one result code.
  WHY: Callers that treat BadChecksum uniformly still converge (next `#`/`$` resynchronizes), so this is benign, but the asymmetry should be documented at the FeedResult contract.
- [S3] tools/debugd/include/target_controller.hpp:42 — EncodeErrno maps DbgErr::kFault to empty reply (wire-"unsupported") rather than an E-code.
  WHY: Per GDB convention empty means "packet unsupported", so a real EFAULT would mislead the host into disabling the feature instead of reporting an error (partial copies bypass this via the int-count path, limiting exposure).
- [S3] Makefile:1398 — hosted-header ban is a denylist that omits non-freestanding headers (<optional>, <variant>, <tuple>, <algorithm> would slip through).
  WHY: Current includes are clean (verified: only array/span/string_view/cstdint/cstddef in headers, <string> correctly banned while <string_view> allowed), but an allowlist inversion would make the Phase-4 portability guard airtight.
- [S3] Makefile:1406 — unrelated drive-by comment change (audit-example CPP scheduler.cpp → task.hpp) bundled into the patch.
  WHY: Behavior-neutral comment edit, but it mixes an out-of-scope hunk into an issue-anchored patch.

DECISION: APPROVED
