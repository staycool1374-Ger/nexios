# AUDIT REPORT 20260921T204504Z
PATCH: audits/pending_patch.diff
FILES: Makefile, mk/rules.mk

## FINDINGS
- [S3] Makefile:425 — stamp comment claims test-full keeps $(ARCH), but test-full IS in _arch_dispatch_goals (Makefile:78) so goal-arch applies to its stamp while its recipe `or` (Makefile:1001) ignores the positional; `make test-full aarch64` stamps aarch64 yet builds x86_64 (recurring redundant clean, self-healing, no wrong artifact; dead positional is pre-existing)
  WHY: Comment/behavior drift only — worst case is one wasted rebuild per invocation, disclosed on the issue, no incorrect build possible
- [S3] Makefile:437 — switch-clean extension relies on BSD/GNU xargs empty-input no-run and `git` failure degradation, verified by reasoning only (no execution per audit constraints)
  WHY: `git ls-files --others --ignored` lists ignored-untracked only (tracked *.elf safe), plain `xargs -0 rm -f` exits 0 on empty input on both BSD/GNU, and `$(shell)` swallows the git-absent failure; all branches degrade to no-op

Verified clean (no finding): (1) _DISPATCH_IN_PROGRESS guard — outer `execute-test aarch64 debug core` word-1 matches so positional debug/release hit `@true` no-ops (Makefile:664,707); all worker recursions (`$(MAKE) release/debug/$(BUILD)/$(KERNEL)` Makefile:970,972,977,980,1028,1251, plus `_do_execute_test` Makefile:948) run with single/file first-goals outside the dispatch set so real targets build; direct `make debug/release` unaffected; run-debug/release-mode sub-makes carry no debug/release goal so the active guard is inert there. (2) ifeq/else/endif balanced at AHCI-335, debug-628/666, release-679/709; AHCI line is prerequisite-only so no branch ever has two recipes. (3) ARCH_EFFECTIVE: explicit ARCH wins via `origin`; goal-arch gated to dispatch first-goals AND SUPPORTED_ARCHS so `make debug/clean/build/-n` parses are behavior-identical; all three $(ARCH)->$(ARCH_EFFECTIVE) substitutions present (433/434/439). (4) Switch-clean runs only inside the arch-changed branch; same-arch incrementals untouched. (5) Glue order-only sysroot edge (rules.mk:249) has correct direction (glue needs sysroot headers), cannot loop (sysroot file targets depend only on the tarball, Makefile:1300), x86-only as scoped. (6) TEMP-token sweep of added lines: zero; diff touches Makefile + mk/rules.mk only, no kernel sources. Critical checks 1-6 (allocs/IrqGuard/assertions/double-free/critical-sections/conditional-semantics): vacuous-or-clean for a build-system-only patch; `_goal_arch` fallback chain can never be empty-undefined. (7) Retrieval artifacts present on #210: work-begun comment (graphify BFS + disposition "too generic, unused", obsidian 0 hits, Makefile line refs) and fix-summary comment (planner sources + dispositions, graphify/obsidian dispositions restated).

DECISION: APPROVED
