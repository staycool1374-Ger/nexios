# AUDIT REPORT 2026-09-25T05-35-47Z
PATCH: audits/pending_patch.diff
FILES: Makefile, scripts/run_all_classes.sh

## FINDINGS
- [S3] Makefile:1140 — timing-model comment still names 250 as the longest expect timeout after TEST_TIMEOUT_RISCV=400 was added.
  WHY: The invariant itself still holds (220 < 400), but the stale parenthetical misdocuments the single source of truth that future timeout edits will rely on.
- [S3] Makefile:1004 — single build-name positional (`make test-full debug`) now resolves _ARCH to the literal word "debug" instead of defaulting to x86_64.
  WHY: _map_arch passes non-matching words through, so positional-first `$(or)` accepts "debug" as arch where the old ARCH-first form fell back to x86_64 — verified via `make test-full debug -n` → `run_all_classes.sh debug debug`, which fails loud at the SUPPORTED_ARCHS guard rather than silently gating a wrong configuration.

DECISION: APPROVED
