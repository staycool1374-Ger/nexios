# AUDIT REPORT 2026-08-30T10-42-48Z
PATCH: audits/pending_patch.diff
FILES: docs/specs/configuration.md, docs/specs/cspace.md, docs/specs/iommu.md, src/kernel/cap/cap_types.hpp, src/kernel/cap/iommu.hpp, src/kernel/cap/iommu.cpp, src/kernel/iommu/vtd.hpp, src/kernel/iommu/iommu.hpp, src/kernel/iommu/iommu.cpp, src/kernel/nexios_config.h, src/kernel/syscall/syscall.hpp, src/kernel/syscall/syscall_handlers_iommu.cpp, src/kernel/test/test_cap_iommu.cpp, src/kernel/test/test_expected_counts.hpp, src/kernel/test/test_registry.cpp

## FINDINGS

- [S2] src/kernel/iommu/iommu.cpp:114-115 (with rollback at :269) — Memory safety / spec §5 rollback violation: `sl_walk(alloc=true)` links each freshly allocated table page into its parent (`*entry = page`) immediately, and on a later allocation failure inside the SAME walk simply returns nullptr — the 1–2 already-linked empty pages stay allocated and referenced forever. `map_frame`'s rollback (`clear_leaf` for q < p) cannot reach them (the failing page has no record, and `clear_leaf` aborts at the first missing link), and `domain_destroy` only frees the root plus record-covered chains — so every PMM-exhaustion-mid-walk event permanently leaks 1–2 pages, unbounded over repeated attempts, violating the binding spec claim (docs/specs/iommu.md §5 "clears every entry written by THIS call … no partial mapping is ever visible"). Tests never exercise PMM exhaustion mid-walk, so the 12/12 pass is consistent and the leak escapes the ResourceTracker zero-delta gate.
  WHY: an error path that leaks PMM pages unboundedly and contradicts the patch's own binding rollback spec is a major defect that must be addressed (no DMA access is granted, hence S2, not S1).
- [S2] src/kernel/iommu/iommu.cpp:86-88, 349, 368, 427 — Out-of-bounds indexing via unvalidated BDF: `PciBdf::device`/`function` are raw `uint8_t` (pci.hpp:33-35), and `ctx_index = device*8 + function` reaches 255*8+255 = 2295, but `g_ctx_tables[slot]` has 256 entries — `attach_device` (write, :349), `clear_attachment` (write, :368) and `context_asr` (read, :427) index out of bounds into adjacent .bss when handed any device > 31 or function > 7, with no range check anywhere on the path.
  WHY: a 2 KiB out-of-bounds .bss write/read in newly introduced manager API designed to receive hardware-derived BDF values is a memory-safety defect that must be fail-closed now, before any phase-2 caller feeds it raw config-space data (not reachable via the iteration-1 syscall surface, hence S2, not S1).
- [S3] src/kernel/iommu/iommu.cpp:364-365 — `clear_attachment` unconditionally zeroes the whole-bus root entry, unbinding the shared per-bus context table for OTHER devices on the same bus (fail-closed overreach: their translation faults; a re-attach restores the root entry). Hardening: zero the root entry only when no other Present context entry remains in that bus table.
  WHY: availability-only imprecision with fail-closed direction — note, no action required for this cycle.

Verified clean (no findings): all manager entry points take `SpinLockGuard` RAII; the leaf-lock discipline holds (no cap/cspace calls, `dispose()`, or `MemPool::free` under `g_lock_`; only leaf PMM alloc/free inside); cap pin/acquire–release balanced on every handler path including all failure exits; `create()` rolls back the claimed domain on MemPool failure; `revoke()` destroys the domain before publishing the revoke; `domain_destroy` is idempotent and single-owner page frees are exclusive (no double-free found); syscall table is compile-time sized (MAX_SYSCALL=61, entries at 59/60 aligned); `#if defined(CONFIG_ARCH_X86_64)` is the established macro with symmetric `#else` shapes and no uninitialized variables; placement-new definition and `g_live_*` RELAXED counter follow the existing irq.cpp/untyped.cpp pattern exactly; expected-count bump 978→990 is purely additive (+12 new tests, no assertion weakened — no Heisenbug masking); registry/expected-counts/class registrations complete and mutually consistent.

## PATCH
`audits/rejected_patch.diff` was written: it makes `sl_walk` unwind (unlink + free) the pages it linked when a later allocation in the same walk fails, and adds `bdf_valid()` range checks to `attach_device`/`clear_attachment`/`context_asr` so an unencodable BDF fails closed instead of indexing out of bounds. Verified with `git apply --check`. Developer must re-run the `cap_iommu` class (and re-audit) after applying; consider adding a PMM-exhaustion-mid-walk regression test so the rollback path is ResourceTracker-covered.

DECISION: REJECTED
