# AUDIT REPORT 20260915-181504
PATCH: audits/pending_patch.diff
FILES: src/kernel/arch/x86_64/hal/apic.hpp, src/kernel/arch/x86_64/hal/shootdown_ipi.cpp, src/kernel/arch/x86_64/hal/shootdown_ipi.hpp, src/kernel/test/test_expected_counts.hpp, src/kernel/test/test_ipi_batching.cpp, test-history.txt

## FINDINGS
- [S3] src/kernel/arch/x86_64/hal/shootdown_ipi.cpp:130 — stored pcid ignored by handler (tlb_flush by VA only)
  WHY: Safe on x86_64 because INVLPG invalidates the VA across all PCIDs, so the per-entry pcid is informational only.
- [S3] src/kernel/arch/x86_64/hal/shootdown_ipi.cpp:137 — applied_/applied_log_ double-count already-flushed entries when CAS-clear loses a race and retries
  WHY: Unreachable under the stated single-producer-per-target contract (self-target fill holds IrqGuard, i.e. mutual exclusion with the IF=0 handler); re-flush itself is idempotent and leftovers stay queued, so only test-only counters could overcount under out-of-contract concurrent append.
- [S3] src/kernel/arch/x86_64/hal/shootdown_ipi.cpp:132 — applied_log_/applied_log_count_ are plain (non-atomic) statics shared across CPUs
  WHY: Unreachable in this patch (test-local self-IPI registration only, no boot change, self target only), but any future boot-wide registration must make the log atomic or per-CPU.

## CHECKS (1-7) disposition
- 1 Dynamic allocation: none (static slots_/log arrays, no new/malloc) — PASS.
- 2 Concurrency: queue_remote fill under IrqGuard with release-publish; handler acquire-load, lock-free, IF=0, no locks/alloc; flush requires IF=1 (documented; tests hold no guard across flush_target) — PASS.
- 3 Assertion masking: counts byte-identical (4,0,0, comment-only); baseline test untouched; 3 stubs converted to real self-IPI tests against deterministic wrapper/handler counters — PASS.
- 4 Memory safety: no PMM/BufferPool touched — PASS.
- 5 Critical-section interference: 0x73 free (only 0x71/0x72 probes, 0xE0/0x80/0xEF/0xEC/0xFF taken; task.cpp 0x73 is bytecode data); class 0x7 in TPR_CLASS_IPC band; EOI exactly once in registrant lambda (mirrors 0x72 probe); SCHED/mailbox path untouched; sorted-insert bounded (pos>0 guard, count<BATCH_SLOTS gate, max write entries[15]); drain-poll bounded 1M spins with queued-entries-kept; CAS retry bounded 3 attempts, leftovers queued never dropped; applied_log 64-entry count guard plus idx bounds check — PASS.
- 6 Preprocessor/conditional semantics: no #ifdef/#ifndef in patch; no asymmetric branches; reset_for_test is test-isolation-only — PASS.
- 7 Retrieval artifacts: issue #159 thread carries the graphify query (IPI/mailbox/APIC → send_ipi + mailbox + TlbShootdown + wake coverage, disposition: used) and vault search (no coverage, disposition: nothing to apply); two-round planner output on thread — PASS per provided thread evidence (GH thread not re-fetched by read-only static audit).

DECISION: APPROVED
