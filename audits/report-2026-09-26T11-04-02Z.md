# AUDIT REPORT 2026-09-26T11-04-02Z
PATCH: audits/pending_patch.diff
FILES: Makefile, docs/specs/debugd.md, src/kernel/arch/aarch64/interrupt_controller.cpp, src/kernel/arch/riscv64/interrupt_controller.cpp, src/kernel/arch/x86_64/hal/idt.cpp, src/kernel/debug/debug_bind.hpp, src/kernel/debug/debug_regs.cpp, src/kernel/debug/debug_regs.hpp, src/kernel/debug/debug_stop.cpp, src/kernel/debug/debug_stop.hpp, src/kernel/kernel.cpp, src/kernel/nexios_config.h, src/kernel/syscall/syscall.hpp, src/kernel/syscall/syscall_handlers_debug.cpp, src/kernel/task/scheduler.cpp, src/kernel/task/scheduler.hpp, src/kernel/task/task.cpp, src/kernel/task/task.hpp, src/kernel/test/test_debug_syscall.cpp, src/kernel/test/test_expected_counts.hpp, src/kernel/test/test_isolate.cpp

## FINDINGS
- [S3] src/kernel/debug/debug_stop.cpp (emu_step_plant) — temp length still sized from the current-pc insn, not the next-VA insn (carried, unchanged)
  WHY: On length-mismatched neighbors the temp overwrites one extra halfword, but snapshot/restore stays byte-exact and the page-edge failure stays fail-closed (parked).
- [S3] src/kernel/debug/debug_stop.cpp (debug_route_fault comment) — comment still claims "nested scheduler_lock_ -> g_stop_lock_" while the code is sequential (park releases before stop_enqueue)
  WHY: Comment-only inaccuracy; the sequential order is the safer discipline, but the comment misdescribes it.
- [S3] src/kernel/debug/debug_stop.cpp (debug_route_fault temp-hit conversion) — STEP event for a consumed temp still carries the breakpoint trap's fault_num (scause 3 / EC 0x3C)
  WHY: Observability only; kind correctly reports STEP and no consumer branches on fault_num for STEP events.
- [S3] src/kernel/debug/debug_stop.cpp + syscall_handlers_debug.cpp — debug_poll_event/debug_dropped_count/debug_drop_target/debug_snapshot_reset/debug_step_complete/debug_drain_debugger still lack CONFIG_DEBUG release stubs
  WHY: Harmless in practice (empty-table ops on unreachable paths) but the same release-symmetry gap as before; drain runs in cleanup() in release on empty tables only.
- [S3] process note — check-7 substance partly referenced rather than pasted (140-node query text and planner plan not inspectable; thread shows 3 comments: 128-node query with used-disposition, vault 0-hits, planner delegation)
  WHY: No absence violation (query + disposition + vault + planner all evidenced on #226), but the second query and plan text itself cannot be inspected on the thread.
- [S3] src/kernel/test/test_expected_counts.hpp — debug_syscall expects 12 on all three arches while aarch64 gates 9/12 (residue filed as #235)
  WHY: Informational only; shared router/queue/shadow/step code is proven 12/12 on x86_64+riscv64 and merge scoping across arches is the owner's call (documented on #226).

## VERIFIED FIXES (prior REJECT dispositions, confirmed in new tree)
- S2 temp-restore APPLIED VERBATIM: debug_bp_consume_temp snapshots va/orig-bytes/len under g_bp_lock and frees the slot there; debug_route_fault restores via bp_write_at outside the lock (fail-open when unmapped); re-entrant faults cannot double-consume the freed slot.
- SIE restore is RISC-V-only (#if defined(CONFIG_ARCH_RISCV64) around the frame[32]/OFF_SSTATUS write with an explicit ELR-corruption comment); no other arch-confused frame indexing in the temp path (emu_step_plant: pc from frame[31] on RISC-V else frame[32], SIE save/mask both RISC-V-guarded; the emu plant/step/continue/rearm functions compile only under RISCV64||AARCH64).
- Temp-restore proven by read-back, never execution-past (step test reads back temp VA orig bytes: riscv +2/c.nop, aarch64 +4/nop); strict-equality asserts throughout (bp bytes, walk stability, per-arch step/fault addrs, 16-then-EAGAIN drain, DEATH kind); park_and_get_pc stale-pc tripwire fails closed (returns 0, park assert fires) instead of planting into the void — hardening, not masking.
- Prior S3s all hold: rearm cleared on non-STEP parks; exit-code fidelity commented (router synthesizes no signals, detach/drain terminate fault-stops with latched exit_code); stval-nonzero-else-sepc mirrors x86-rip/aarch64-ELR; emu generalizes riscv+aarch64 (RVC decode vs fixed-4B, no SS/MDSCR); poll snapshots ownership first (bind released, queue-lock-only scan); exact-phys everywhere (bp paths, read/write_mem, test helpers + staging); [[maybe_unused]] on release-stubbed helpers; LTO mode stamp (recipe-time, same doctrine as ARCH_STAMP); IDT #BP gate DPL3 0xEE; kUserYieldStubVa single canonical definition in task.hpp.
- NEW deltas reviewed, no safety defect: control-teardown recycle hardening (id capture + died-loop + conditional reap only when valid/ours/not-REAPED — neither leaks nor double-frees in any branch; system-reclaimed pages intentionally not re-freed per the teardown-reclaims doctrine stated at the overflow drain); aarch64 sled/step/fault/overflow mechanics (2-nop sled, temp at +4, unmapped-redirect faults, all-three-slot brk poison) bounded and fail-closed; spec §4 deviation notes are docs-only; Makefile/test-only changes (LTO stamp, 12-counts, isolation resets) touch no runtime locking.

DECISION: APPROVED
