# AUDIT REPORT 2026-09-22T091500Z
PATCH: audits/pending_patch.diff
FILES: linker/linker_riscv64.ld, src/kernel/arch/hal/pci.hpp, src/kernel/arch/riscv64/boot.S, src/kernel/arch/riscv64/hal/pci_impl.hpp, src/kernel/arch/riscv64/syscall_entry.S, src/kernel/arch/riscv64/test_riscv64.cpp, src/kernel/kernel.cpp, src/kernel/memory/pmm.cpp, src/kernel/test/test_isolate.cpp

## FINDINGS
- [S2] src/kernel/arch/riscv64/boot.S:26-27 — `li t1, 0x80C00000` does not materialize 0x0000000080C00000: RV64 LUI sign-extends bit 31, and the low 12 bits are zero so this assembles to a single `lui t1,0x80C00`, yielding 0xFFFFFFFF80C00000 (same sign-extension the file itself relies on at lines 160-162 for 0xFFFFFFC0); the unsigned `bltu t0,t1,10f` against the exact-valued `la t0,kernel_phys_end` (~0x80xxxxxx) is therefore always-taken and the fail-stop is dead code — the prior S2 (no effective collision guard) is NOT fixed, only moved to runtime form
  WHY: single-LUI sign-extension puts the threshold above every realistic 32-bit image-end value in the unsigned domain, so breach can never fall through to the SBI-shutdown path
- [S3] src/kernel/arch/riscv64/boot.S:26 — 2MB metadata slack hardcodes ~1GB RAM ceiling (PMM metadata ~8.25B/page ⇒ 2MB covers ~254K pages ≈ 990MB; configured 256MB needs ~540KB, 3.8x margin OK) but larger RAM configs would overflow the stack while still passing the check
  WHY: the slack is a build-target constant with no derivation from the actual RAM size, so the guard silently under-covers any future memory-size bump
- [S3] src/kernel/test/test_isolate.cpp:340-341 — "Nothing on riscv64 modifies boot tables / VMM backend is a no-op" premise still wrong, unchanged from prior reject
  WHY: vmm.cpp has a real Sv39 map_page branch that only skips setting the modified flags, and riscv64_sv39_block_split splits HHDM blocks at runtime — the arch-gate is correct but its justification misstates the mechanism
- [S3] src/kernel/arch/riscv64/hal/pci_impl.hpp — PCI_ECAM_SIZE 4GB→256MB correction still lands in a TU included by nothing in src/ (dead fallback, now honestly commented but effect-free)
  WHY: the live base is solely hal/pci.hpp pci_make_addr, so this hunk changes no executable behavior

## VERIFIED CLEAN (re-audit record)
- (claim 1) pmm.cpp:800-806 matches rejected_patch.diff semantics content-verbatim (rebuild_free_list() with identical rationale comment); called under the IrqSpinLockGuard held since line 786, rebuild_free_list() is noexcept/alloc-free and only relinks bitmap-clear pages while reserved pages are bitmap-set with free_pages_ pre-decremented — S1 fixed, checks 1/2/4 clean
- (claim 3) kernel.cpp:277-280 `if (dtb_phys != 0)` guard restores old skip semantics exactly (dtb==NULL ⇒ `if (dtb && ...)` skips) — S3 fixed
- (boot.S check, minus S2 above) control flow confirmed: `bltu t0,t1,10f` taken=continue/fallthrough=fail as claimed; `la t0,kernel_phys_end` loads the absolute-symbol value (not a deref), correct; pre-stack code touches only t0/t1 (s0/s1 preserved, a0/a1 re-derived from s0/s1 at jalr, fail-path a0/a7 clobbers die in shutdown); SBI putchar-eid-1/shutdown-eid-8 valid pre-MMU; labels 9/10 unique in file (0/1/2 reuse is GAS-local with explicit b/f refs, pre-existing)
- (claim 2, partial) runtime fail-stop is an acceptable substitute direction (fail-safe shutdown, no silent scribble) for the configured 256MB target given ld rejects ASSERT — but void as implemented (S2 above); link-time detection loss remains even after fix (defect escapes to first boot, not build)
- CRITICAL 1-6: no heap allocs; reserve_range+rebuild under pmm_lock_, single-hart pre-scheduler boot asm; no assertion changes in tests (sv39 HHDM alias is an address-domain fix, not masking); no free paths touched; boot-table edits additive; #if gates symmetric and defined()-safe
- CRITICAL 7: retrieval artifacts present (work-begun comment 5772124757: graphify BFS + vault 0-hits with dispositions; hypothesis chain evidence-proven); re-verified gates recorded on issue (comment 5773150750: riscv idle 506 ticks, x86 task_core 6/6, arch_aarch64 30/30, build Errors 0)

## PATCH
audits/rejected_patch.diff written: zero-extend t1 via slli/srli 32 after the li so the bltu threshold compare runs in the true physical domain (one sentence: makes the fail-stop actually fire).

DECISION: REJECTED
