# AUDIT REPORT 20260922T132738Z
PATCH: audits/pending_patch.diff
FILES: src/kernel/arch/hal/context.hpp, src/kernel/arch/riscv64/test_riscv64.cpp, src/kernel/memory/vmm.cpp, src/kernel/test/test_expected_counts.hpp

## FINDINGS
- [S3] src/kernel/memory/vmm.cpp:729 — nested `#if defined(CONFIG_ARCH_RISCV64)` inside an already riscv64-gated block is redundant
  WHY: Harmless duplication (no semantic asymmetry, both branches return phys), style-only cleanup candidate.
- [S3] src/kernel/memory/vmm.cpp:526 — riscv `map_page_in_pml4` on a cloned table with a low-half VA would write into the now-shared L0[0] L2 without a block-split guard, affecting all address spaces
  WHY: No in-tree caller maps low VAs into task tables (task mappings are high-half), so unreachable today; deserves a guard or assert when #152 lands the Sv39 backend.
- [S3] src/kernel/arch/riscv64/rtc.cpp:20 — driver comment claims "wall-clock time" while the implementation returns mtime-uptime seconds-since-boot rendered from a 1970 epoch
  WHY: The ==1970 test pin is honest for the current driver, but the misleading comment should be fixed to "uptime clock" so the future goldfish-rtc wall-clock work is not confused.
- [S3] src/kernel/test/test_cross_arch.cpp:233 — riscv64 branch still probes `stack_top[-i]` (by-value semantics) after the init_stack by-ref change; correct slots are now `stack_top[0..19]` as the aarch64 branch already does
  WHY: Fail-closed test-only staleness, unreachable in the arch_riscv64 gate (separate arch_cross class, not run here); fix as follow-up before arch_cross runs on riscv64.
- [S3] src/kernel/memory/vmm.cpp:735 — L0[0] sharing assumes SSTATUS.SUM stays 0 for #206 U-mode safety (U=0 leaves verified via boot.S leaf flags 0xEF, U bit 4 clear)
  WHY: Holds under the current S-mode-only runtime; #206 U-mode entry must pin SUM=0, recorded here as an explicit precondition.

## VERIFICATION SUMMARY (issue #205 scope)
- (A) L0[0] copy: boot.S leaf flags 0xEF prove U=0 on all shared leaves; non-leaf L0[0] is V-only (no U bit applies); sharing mirrors x86/aarch64 kernel-half sharing by design; kernel-table runtime edits propagating to clones is intended global-MMIO behavior.
- (B) init_stack: all callers are tests (test_hal x86-guarded, test_cross_arch, test_riscv64); 20-slot layout (4 header + 16 regs) matches frame[15..19] asserts exactly.
- (C) Deregistrations justified: misa/mvendorid/marchid/mimpid/medeleg/mideleg reads in test bodies execute in S-mode where they fault illegal-insn (M-only CSRs); map_unmap/block_split depend on ArchPageTable riscv backend which decodes PTEs x86-style (`& ~0xFFF` masking, phys-as-ptr, l2[l1_idx] block indexing — page_table_impl.hpp:175-342). plic_init unmask(10)-first is side-effect-free for the later claim==0 check (no pending source; complete-without-claim is benign). RTC ==1970 pin matches the mtime-uptime implementation + monotonicity retained.
- (D) Count: 16 active JARVIS_REGISTER_TEST lines counted in register_riscv64_tests() == {0,0,16}. TEMP-token sweep: zero. No test-count mismatch.
- Checks 1-6: no heap allocs, no concurrency boundaries touched, no Heisenbug masking (both assertion changes correct previously-impossible assertions), no PMM/BufferPool ownership change, no critical-section interference, preprocessor nesting redundant-only.
- Check 7: retrieval artifacts present on issue #205 (graphify BFS query + dispositions and 0-hit vault search pasted in the work-begun comment 5777045287; triage notes in 5777147040/5777215917/5777358214).

DECISION: APPROVED
