# AUDIT REPORT 2026-09-22T083000Z
PATCH: audits/pending_patch.diff
FILES: linker/linker_riscv64.ld, src/kernel/arch/hal/pci.hpp, src/kernel/arch/riscv64/boot.S, src/kernel/arch/riscv64/hal/pci_impl.hpp, src/kernel/arch/riscv64/syscall_entry.S, src/kernel/arch/riscv64/test_riscv64.cpp, src/kernel/kernel.cpp, src/kernel/test/test_isolate.cpp

## FINDINGS
- [S1] src/kernel/memory/pmm.cpp:783 (via src/kernel/kernel.cpp:757 contract) — PMM::reserve_range() never unlinks reserved pages from the O(1) free list, so the patch's live-stack pin is void on the dominant alloc path
  WHY: rebuild_free_list() runs once inside PMM::init (pmm.cpp:150) before the reserve call, and try_alloc_kernel(1)/try_alloc_user(1) pop free_head_ with no bitmap_test (pmm.cpp:199-207,252-260), a path alloc_page() itself uses (pmm.cpp:395); reserved stack indices 0x80E00-0x80FFF stay linked near the list head and are handed out after ~32K net single-page allocs, double-allocating the live BSP stack.
- [S2] linker/linker_riscv64.ld:115-128 — fixed-address NOLOAD .boot_stack_rv has no collision guard and the comment falsely claims ASSERTs exist
  WHY: a NOLOAD section at 0x80E00000 never advances phys_lma, so future image growth past kernel_phys_end silently overlays the live boot stack at link time with no error; the comment's "The ASSERTs guard image growth" references guards that are absent from the diff.
- [S3] src/kernel/kernel.cpp:672-679 — null-DTB edge now aliases to HHDM base instead of skipping
  WHY: dtb_ptr==0 previously yielded dtb==NULL and skipped via `if (dtb && ...)`; after `dtb_phys += HHDM_OFFSET` the pointer is nonzero (0xFFFFFFC000000000, L0[256] unmapped) and fdt_check_header faults — latent only because OpenSBI always passes a DTB, but the null guard was silently defeated.
- [S3] src/kernel/test/test_isolate.cpp:582-587,639-641 — comments misdescribe the riscv64 VMM backend as "no-op"/"x86-shaped"
  WHY: vmm.cpp has a real Sv39 map_page branch (vmm.cpp:224-270) that simply never sets hhdm_modified_/identity_modified_, which is what actually keeps the restores unfired; "nothing modifies boot tables" is additionally contradicted by riscv64_sv39_block_split, which splits HHDM 2MB blocks at runtime (benign, PMM-restore covered, but the premise is wrong).
- [S3] src/kernel/arch/riscv64/hal/pci_impl.hpp:37-44 — PCI_ECAM_SIZE fix lands in a file never included anywhere in src/
  WHY: no TU includes *pci_impl.hpp (only @file self-mentions), so the dead-fallback claim is true but understated — the whole TU is dead and the 4GB→256MB correction is effect-free; live path is solely hal/pci.hpp pci_make_addr.
- [S3] src/kernel/task/scheduler.cpp:3259 — is_boot_stack_rsp() can never fire on riscv64, before and after this patch
  WHY: it tests [_stack_start,_stack_end) (the 16KB .boot_stack section, kernel.cpp:1991), while the BSP runs on .boot.bss _boot_stack (pre-patch) and .boot_stack_rv (post-patch) — behavior identical across the patch, but the H2 harness-on-boot-stack path is unreachable on riscv64 either way.

## VERIFIED CLEAN (spot-check record)
- (B) L1_id leaf math: index (top>>21)&0x1FF*8 = 7 for 0x80E00000/0x80F00000, PPN = (base & -0x200000)>>2 with sign-extended mask correct, `or` with 0xEF leaf flags non-overlapping; ECAM L2[384..511] slots/PPN/count (384*8=3072, 0x30000=0x30000000>>12, 128x2MB=0x30000000-0x3FFFFFFF) disjoint from PLIC(96,97)/UART(128) and inside L0[0] 1GB window; .boot size preserved byte-exact (4KB reserved replaces 4KB stack); NOLOAD excluded by `objcopy -O binary` (Makefile:657), VMA==LMA pre-transition, HHDM-covered post-transition (0x80E00000 in 128x2MB HHDM window).
- (A) reserve_range sits immediately after PMM::init with no intervening PMM alloc (kernel.cpp:751-758); DTB 0x8FE00000 inside HHDM RAM window (boot.S:56-68).
- (D) t0/t1 reloaded at 7f after use, MODE-or (phys>>12)|(8<<60) matches write_cr3 (io_impl.hpp:187-189), sfence.vma retained.
- (E) x86/aarch64 pci.hpp branches byte-identical in behavior (aarch64 base unchanged); include-order proof: hal/pci.hpp owns the only live CONFIG_PCI_ECAM_BASE definition (pci.hpp:148-154, dispatcher kernel/arch/pci.hpp:30).
- (F) gates use defined() (macros always defined as 1, never 0 — nexios_config.h:34-50, Makefile -D bare); (void) casts riscv-only on truly-unused vars; restores stay flag-gated and flags are unsettable on riscv (Sv39 branch sets none).
- (G) constants.hpp include present, HHDM_OFFSET=0xFFFFFFC000000000 on riscv (nexios_config.h:159-161); neighboring raw-phys reads (PLIC/UART/ECAM-identity) all land in mapped identity leaves — no next-fault candidate.
- (8) TEMP sweep zero in src/ for TEMP-DIAG/diag_boot_tables/TEMP-DBG/PRE-FIX; patch adds no `#if 0` (pre-existing ones unrelated to patch files).
- (9) Retrieval artifacts on issue #29: work-begun comment pastes graphify BFS query with dispositions + vault 0-hits; hypothesis chain evidence-proven (objdump/xp/monitor/diag).
- CRITICAL 1-5: no heap allocs added; pmm_lock_ held in reserve_range, single-hart boot asm; no assertion weakening; no free paths touched; trap-return preserves sfence + flag clear, boot-table edits purely additive (L1_id[7], L2[384..511] previously zero).

## PATCH
audits/rejected_patch.diff written: (1) rebuild_free_list() at end of PMM::reserve_range so reservations hold on the O(1) single-page paths; (2) linker ASSERT (_boot_stack_rv_low >= kernel_phys_end) plus stale-comment correction.

DECISION: REJECTED
