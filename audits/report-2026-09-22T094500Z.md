# AUDIT REPORT 2026-09-22T094500Z
PATCH: audits/pending_patch.diff
FILES: linker/linker_riscv64.ld, src/kernel/arch/hal/pci.hpp, src/kernel/arch/riscv64/boot.S, src/kernel/arch/riscv64/hal/pci_impl.hpp, src/kernel/arch/riscv64/syscall_entry.S, src/kernel/arch/riscv64/test_riscv64.cpp, src/kernel/kernel.cpp, src/kernel/memory/pmm.cpp, src/kernel/test/test_isolate.cpp

## FINDINGS
- [S3] src/kernel/arch/riscv64/hal/pci_impl.hpp — PCI_ECAM_SIZE 4GB→256MB correction still lands in a TU included by nothing in src/ (dead fallback, now honestly commented as such); live base remains hal/pci.hpp pci_make_addr
  WHY: the hunk changes no executable behavior, only the comment is now honest about it
- [S3] src/kernel/arch/riscv64/boot.S:26-28 — collision guard is runtime fail-stop (first boot) not build-time: this ld rejects ASSERT() here, so image growth escapes to boot-time SBI shutdown rather than a link error
  WHY: detection latency is a hardening note, not a correctness violation — the guard itself now fires correctly (see verified-clean)

## VERIFIED CLEAN (re-audit #2 record, issue #29)
- (claim 1) verbatim hunk CONFIRMED: pending_patch.diff boot.S lines 70-75 are byte-identical to audits/rejected_patch.diff lines 8-13 (LUI sign-extension comment + `slli t1,t1,32` / `srli t1,t1,32`); `0xFFFFFFFF80C00000<<32>>32 = 0x0000000080C00000`, so the unsigned `bltu t0,t1,10f` now compares in the true physical domain against `la t0,kernel_phys_end` — prior S2 (dead fail-stop) FIXED
- (claim 2a) test_isolate comment now accurate: lines 347-350 cite the exact mechanism (Sv39 map_page/map_page_in_pml4 branches set no hhdm/identity-modified flags — confirmed at vmm.cpp:224-270 and 526-553, flag stores exist only in the non-riscv branches) so the flag-gated restores stay unfired; defers the real Sv39 save/restore backend to #152 — prior S3 premise FIXED
- (claim 2b) boot.S slack comment now accurate: lines 63-67 derive the ceiling (~990MB from 2MB/8.25B-per-page: 2097152/8.25 = ~254K pages x 4KB ~= 993MB) and state larger RAM configs must move the stack — prior S3 FIXED
- (claim 3) pci_impl dead-TU note accepted as-is (S3 above, effect-free by construction); no other diff content changed since 091500Z review — same 9 files, all previously-verified hunks (pmm rebuild_free_list, kernel dtb guard, syscall SATP PPN+mode, sv39 HHDM test alias, ECAM base/leaves, linker NOLOAD stack) structurally identical
- CRITICAL 1-6 over the delta: two ALU reg-ops on t1 pre-stack/pre-MMU + comment-only changes — no heap allocs (1); single-hart pre-scheduler asm, PMM rebuild still under pmm_lock_ as previously verified (2); no assertion changes (3); no free paths touched (4); boot-table edits additive, fail-path clobbers only a0/a7 dying in SBI shutdown (5); no #ifdef touched by the delta (6)
- CRITICAL 7: retrieval artifacts carried over from prior review (work-begun comment with graphify BFS + vault dispositions, hypothesis chain); delta is auditor-dictated + comment corrections requiring no new retrieval; re-verified gates restated on the issue per developer claim (riscv ticks/dispatch, x86 task_core 6/6, arch_aarch64 30/30, build Errors 0)

DECISION: APPROVED
