# AUDIT REPORT 2026-09-26T10:19:01Z
PATCH: audits/pending_patch.diff
FILES: Makefile, docs/specs/debugd.md, src/kernel/arch/aarch64/interrupt_controller.cpp, src/kernel/arch/riscv64/interrupt_controller.cpp, src/kernel/arch/x86_64/hal/idt.cpp, src/kernel/debug/debug_bind.hpp, src/kernel/debug/debug_regs.cpp, src/kernel/debug/debug_regs.hpp, src/kernel/debug/debug_stop.cpp, src/kernel/debug/debug_stop.hpp, src/kernel/kernel.cpp, src/kernel/nexios_config.h, src/kernel/syscall/syscall.hpp, src/kernel/syscall/syscall_handlers_debug.cpp, src/kernel/task/scheduler.cpp, src/kernel/task/scheduler.hpp, src/kernel/task/task.cpp, src/kernel/task/task.hpp, src/kernel/test/test_debug_syscall.cpp, src/kernel/test/test_expected_counts.hpp, src/kernel/test/test_isolate.cpp

## FINDINGS
- [S3] src/kernel/debug/debug_stop.cpp (emu_step_plant) — temp-breakpoint length sized from the current-pc insn, not the next-VA insn
  WHY: When the two lengths differ the temp overwrites one extra halfword, though restore is byte-exact and page-edge failure stays fail-closed (parked).
- [S3] src/kernel/debug/debug_stop.cpp (debug_continue plain-resume path; debug_step) — stale debug_rearm_va not cleared on non-STEP parks/resumes
  WHY: A fault intervening in a step-over window leaves rearm set, so a later STEP completion re-inserts a breakpoint the debugger never asked for (fail-safe direction: extra stop, never runaway).
- [S3] src/kernel/syscall/syscall_handlers_debug.cpp (detach sel0 fault path; debug_drain_debugger fault path) — fault-stop terminate uses the latched tgt->exit_code, never set by the router
  WHY: The task still terminates per the FAULT-only disposition, but the exit code lacks the signal-derived value an unattached fault would carry (observability, not safety).
- [S3] src/kernel/arch/riscv64/interrupt_controller.cpp (fault routing) — inline comment claims stval for FAULT stops but code passes sepc
  WHY: Comment/code mismatch only; the header contract explicitly allows pc as the stop VA, and the test encodes sepc strictly (no masking).
- [S3] process note — aarch64 gates red at audit time (9/12 per #226 10:14Z; fault-routing/overflow/poll-empty isolated to #235, x86_64+riscv64 12/12 green, no assertion masking found)
  WHY: Static analysis of the patch shows no defect explaining it (shared router proven on two arches; residue points at resume-frame/QEMU behavior outside these lines), but VERIFIED (0 failures) cannot be claimed on aarch64 until #235 closes — merge scoping is the owner's call.

DECISION: APPROVED