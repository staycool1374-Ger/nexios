diff --git a/src/kernel/test/test_cross_arch.cpp b/src/kernel/test/test_cross_arch.cpp
--- a/src/kernel/test/test_cross_arch.cpp
+++ b/src/kernel/test/test_cross_arch.cpp
@@ -899,16 +899,8 @@
         for (size_t k = 0; k < 64; ++k) {
             if (cross_is_huge(pd[k]))
                 continue;
-            // TEMP-DBG #199: characterize non-huge PD[0..63].
-            uint64_t const entry = pd[k];
-            bool const tracked =
-                (entry & PRESENT) &&
-                PMM::is_allocated(entry & cross_frame_mask());
-            Logger::error("TEARDOWN-PD64 half=%lu idx=%lu val=%lx "
-                          "tracked=%u",
-                          half, k, entry, (unsigned)tracked);
             uint64_t const pt_page = pd[k] & cross_frame_mask();
             JARVIS_ASSERT_FMT(
                 (pd[k] & PRESENT) && PMM::is_allocated(pt_page),
                 "PD[%lu] must be huge or a tracked PT (half %lu)", k, half);
         }
