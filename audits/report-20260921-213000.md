# AUDIT REPORT 20260921-213000
PATCH: audits/pending_patch.diff
FILES: src/kernel/arch/aarch64/boot.S, src/kernel/arch/aarch64/hal/pci_impl.hpp, src/kernel/arch/aarch64/interrupt_controller.cpp, src/kernel/arch/aarch64/vectors.S, src/kernel/arch/hal/pci.hpp

## FINDINGS
- [S3] src/kernel/arch/aarch64/vectors.S:103 — missing `.extern aarch64_el1_unexpected_fault` (el0 handler has one at line 217)
  WHY: Cosmetic inconsistency only; GAS resolves the undefined `bl` via relocation and the `extern "C"` definition in interrupt_controller.cpp provides the exact unmangled symbol, so linkage is sound.
- [S3] src/kernel/arch/riscv64/hal/pci_impl.hpp:39 — riscv shadow `PCI_ECAM_SIZE` still `0x100000000ULL` (4GB) while aarch64 shadow corrected to `0x10000000ULL`
  WHY: Pre-existing drift outside this patch (untouched file, zero consumers of either SIZE constant tree-wide), hence no safety impact; flag for hygiene only.
- [S3] process — issue #216 thread carries no separately pasted graphify/vault query; retrieval relies on stated shared-patch linkage to #214
  WHY: #214 thread shows graphify BFS (exception-table-audit spec, dispositioned) + obsidian 0-hits on both work-begun and completion comments, and #216 documents hardware-grounded evidence (`info mtree`, real vendor 0x1B36) plus shared-patch linkage, so check 7 is substantively satisfied; note the linkage asymmetry only.

## VERIFICATION SUMMARY (recomputed from scratch, not trusted from comments)
- vectors.S: exactly 2 slots retargeted (EL1t-sync L16, EL1h-sync L26); el1_irq/el0_sync/el0_irq/FIQ/SError/AArch32 lines byte-identical. Stub stashes x0-x3+x30 (48B, 16-aligned) before use; adrp/:lo12: idiom identical to proven default_exception (same TU, ±4GB reach OK); (esr,far,elr) marshalled in x0-x2; `bl aarch64_el1_unexpected_fault` matches `extern "C"` definition exactly (namespace does not mangle C-linkage names). Return path pops 48B LIFO (x30, x2/x3, ELR+=4, x0/x1) then erets — balanced; x3 stash is memory-resident so callee clobber of caller-saved x3 is harmless.
- C handler: only `Serial::puts/putchar` (bounded-poll TX cap + byte-drop + atomic counter, no Logger/heap/locks); `#ifdef CONFIG_DEBUG` panic-vs-return is symmetric with `(void)` casts and matches #214 acceptance (debug panic); `panic` declared `extern "C" noreturn` in kernel/kernel.hpp, included by the TU.
- boot.S ECAM: VA 0xFFFF804010000000 = HHDM(0xFFFF800000000000)+PA(0x4010000000) ✓; L0=256 ✓, L1=256 (slot base 0xFFFF804000000000, L1 base+256*8=0x40026800 ✓), L2 start 128 (+0x10000000/2MB) ✓; 128 entries ×2MB = 256MB exactly (`cmp 256/b.lo`) ✓; flags 0x421 match UART/GIC precedent ✓; last PA 0x401FE00000, block ends 0x401FFFFFFF = window end exactly, no overflow; base 256MB-aligned so OR==ADD, no ECAM sub-decode carry ✓; page 0x40029000 was next-free (0x40020000-0x40028000 claimed) ✓; dc range 9→10 covers 0x40029000 ✓; L1[256] never previously programmed (only L1[0]/L1[1] written), L2[0..127] zeroed fail-closed ✓; labels 211/212 unique; x20 (DTB) preserved; x11/x15 re-init before reuse ✓.
- pci.hpp/pci_impl.hpp: aarch64 base 0x4010000000 in both (no drift); riscv fallback 0x100000000 byte-identical; HHDM alias strictly inside `#if CONFIG_ARCH_AARCH64`, riscv else-branch byte-identical to old line; x86 `#if` branch untouched; `<constants.hpp>` pulls only types.hpp+nexios_config.h (no include cycle); PCI_ECAM_SIZE 0x100000000→0x10000000 corrects 4GB→256MB to match the 256*32*8*4KB arithmetic.
- TEMP/probe sweep: zero tokens in patch and in aarch64 tree; no test files touched (counts unchangeable by this patch); g_debug_elr new .globl+.quad, zero-init, no collisions.
- Behavior deltas: (a) EL1 sync dumps+panics in debug, legacy skip in release — intended per #214; (b) aarch64 PCI reads now real MMIO — intended per #216; (c) riscv64 — no behavior change (base preserved, alias excluded, header-only include addition).
- CRITICAL CHECKS 1–7: (1) no heap allocation; (2) exception-masked context, no locks taken, atomics only — no IrqGuard violation; (3) no test/assertion edits; (4) no PMM/BufferPool interaction; (5) disjoint L1/L2 slots and vector entries, no invariant interference; (6) CONFIG_DEBUG and ARCH if/else branches symmetric, no uninit paths; (7) retrieval artifacts present (#214 pasted graphify+obsidian+planner dispositions; #216 shared-patch linkage + hardware evidence) — SATISFIED.

DECISION: APPROVED
