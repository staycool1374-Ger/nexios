# AUDIT REPORT 20260921T185406Z
PATCH: audits/pending_patch.diff
FILES: src/kernel/kernel.cpp, src/kernel/test/test_preemption.cpp, src/kernel/test/test_expected_counts.hpp

## FINDINGS
- [S3] src/kernel/test/test_preemption.cpp:348-360,404-432 — partial SwSlots restore (test 1 restores save/load/id only; test 2 restores save/id only; load_cr3_from/load_tls_from/load_kstack_base-top/generation left dirty)
  WHY: Inert only because save_rsp_to==null gates every apply path and restore_state clears BSP atoms at the test boundary; a future epilogue consuming load-side slots without the save gate would inherit stale frames.
- [S3] src/kernel/test/test_preemption.cpp:301-303,394-396 — unbounded pause-spins waiting for parent BLOCKED (no tick/iteration bound)
  WHY: Matches the file's established precedent (lines 54,77,144) and is globally bounded by the harness watchdog, but a never-blocking parent wedges the class until the watchdog fires instead of failing fast.
- [S3] src/kernel/test/test_preemption.cpp:347-360,404-432 — slot restore performed outside the IrqGuard after in-guard publish+reads
  WHY: A timer tick landing in the guard-release/restore gap could publish its own arm and have it clobbered by the stale-prior restore; recovery is level-triggered (needs_switch re-arms), so the worst case is one lost tick, not a strand.
- [S3] process-note — mandatory-retrieval artifacts (graphify + obsidian queries on issue #212) asserted by developer but unverifiable by static audit
  WHY: Per task instruction this is recorded and is not a reject reason alone; code findings above are all S3.

## VERIFIED (no finding)
- Gate is exactly `#if defined(CONFIG_ARCH_AARCH64)` (kernel.cpp:985); x86/riscv fall through to the original `return ret` — zero shared-path semantic change; switch_away_from_terminating itself untouched, so x86 production delta is nil.
- TERMINATED excluded by the `state == BLOCKED` predicate; sys_exit owns the TERMINATED arm (syscall_handlers_misc.cpp:106); states are mutually exclusive and switch_away early-returns if save_rsp_to already armed (scheduler.cpp:4082) — no double-publish hazard.
- Null-current guarded (`cur != nullptr`, kernel.cpp:997).
- Lock discipline: zero scheduler_lock_ acquisitions in src/kernel/syscall/ (all scoped guards, none surviving to return); bridge enters switch_away lock-free; the lock-then-release-before-IrqGuard ordering inside switch_away is unchanged pre-existing behavior identical to the sys_exit call context — no new lock-ordering edge.
- IRQ state: SVC entry masks DAIF; publish step uses mask-preserving IrqGuard after scheduler_lock_ release — cannot deadlock; handlers add no sti/cli imbalance.
- BLOCKED-caller reuse walk (scheduler.cpp:4074-4208): RUNNING-requeue branch correctly skipped for non-RUNNING; next_task() skips non-READY/non-RUNNING plus explicit next==&exiting fallback to own_idle so the BLOCKED caller is never selected; CR0.TS is x86-compiled-out on the only arch reaching the new call; TSS/IOPB/CR3/TLS publishes are already exercised on aarch64 via the sys_exit path; aarch64 frame validation branch present.
- Lost wakeup (check 1): no asm change and none needed — syscall_entry.S:79-82 already branches to irq_context_switch_common on save_rsp_to!=0, and irq_context_switch_common clears the arm (vectors.S:229-231); release-ordered publish + generation bump preserved.
- Counts arithmetic: scheduler_preemption 11→13 (+2 tests, +2 REGISTERs), core 439→441 (+2); core aggregate includes run_scheduler_preemption_group (test_registry.cpp:975), proc_elf does not; aarch64/riscv columns stay 0 (= validation disabled, consistent with prior #208 precedent).
- TEMP-token sweep: zero TEMP/FIXME/XXX/HACK/TODO tokens in any added line (pre-existing tokens elsewhere are out of scope).
- Teardown balances: terminate_and_drain/terminate/drain per created task, matching file precedent; task_at(0)==idle_task_ asserted non-null before use.

DECISION: APPROVED
