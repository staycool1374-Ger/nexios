# REJECTED PLAN #74 — corrected steps (apply verbatim, then re-audit)

1. `task.hpp`: add `uint64_t tls_base_;` beside `iopb_slot_` (542) + `tls_base_(0)` in ctor init-list beside `iopb_slot_(IOPB_SLOT_NONE)` (278). Unchanged.
2. `task.cpp`: after each `iopb_slot_=IOPB_SLOT_NONE` (915/1159/1357) insert `tcb->tls_base_ = 0;`; in `clone` only, after the inherit block add `tcb->tls_base_ = parent->tls_base_;`. Unchanged.
3. `elf.cpp finalize_loaded_task`: after line 514 insert `tcb->tls_base_ = 0;`. In `exec_into_current` (elf.cpp:715), after the address-space swap succeeds, set `tcb->tls_base_ = 0` AND, if exec target is the current task, live-clear under the matching `CONFIG_ARCH_*` guard (x86_64 `arch::wrmsr(MSR_FS_BASE, 0)`; aarch64 `TPIDR_EL0=0`; riscv64 `OFF_TP` save area to 0) so no stale base survives exec.
4. `syscall_errors.hpp`: use `X(TLS_INVALID_BASE, 109, "Invalid TLS base address")` (109 is the true first free ID above the 101-108 task band); confirm no error-count assertion needs updating.
5. `msr_impl.hpp`: add `MSR_FS_BASE = 0xC0000100` beside `MSR_GS_BASE`. Unchanged. `aarch64/hal/io_impl.hpp`: add `write_tpidr_el0` with trailing `isb()` beside `write_ttbr0_el1` (251-255). `riscv64/hal/io_impl.hpp`: add `write_tp` via `mv tp` beside the satp helpers (167-195).
6. Publish slot: add `scheduler_load_tls_from[CONFIG_MAX_CPUS]` + `SwSlots::load_tls_from()` mirroring `load_cr3_from` (mirror scheduler.hpp:1051-1057 decl block incl. the NOLINTNEXTLINE comment); extend capture_percpu/restore_percpu, cancel_pending_switch_cpu (605), BSP clear (4192), clear_switch_globals (4209) AND every other load_cr3_from clear site — scheduler.cpp:947,1802,1827,1984,2013,3810,4674 — each new clear placed directly beside its CR3 clear with the same memory order.
7. New `syscall_handlers_tls.cpp`: validate (0 clears; else per-arch gate — x86_64 48-bit `is_canonical()` + `arg0 < CONFIG_USER_SPACE_LIMIT`; aarch64 LIMIT-subsumed; riscv64 explicit `(arg0>>39)==0` + LIMIT — fail with `(uint64_t)-SYS_ERR_TLS_INVALID_BASE`, never ENSURE); order: fetch current, null → `(uint64_t)-SYS_ERR_SCHED_NO_CURRENT`, then validate, then `Scheduler::set_tls_base_err`; handler maps `SchedulerError::NO_CURRENT` → `(uint64_t)-SYS_ERR_SCHED_NO_CURRENT`. Never touch `scheduler_lock_` directly from Syscall. No LA57 live-query (zero implementation symbols tree-wide; trigger condition recorded: widen gate if LA57 lands). Keep 85 out of `k_syscall_fast[]`.
8. New `Scheduler::set_tls_base_err(TaskControlBlock &task, uint64_t base)` (decl scheduler.hpp:240 beside `set_affinity_err`, same `errors::SchedulerError … noexcept` signature): `arch::IrqGuard` + `SpinLockGuard(scheduler_lock_)` (no `quiesce_enter()` — OMIT confirmed: AP apply reads the per-CPU slot atomically, never the TCB; no check-then-act on remote state, unlike affinity); re-fetch `current_task()`, null → `NO_CURRENT`; store `task.tls_base_ = base`; release lock; then live-load ONLY if `&task == current_task()` (this-CPU gate, not `is_current_on_any_cpu`) — x86_64 WRMSR FS_BASE; aarch64 TPIDR_EL0; riscv64 `write_tp`. Base 0 clears the same way.
9. `scheduler.cpp switch_to_task` (~3599-3635) AND the second publish site (~3972-3992): after each CR3 publish if/else insert `__atomic_store_n(&SwSlots::load_tls_from(), (next.is_user_ ? next.tls_base_ : 0), __ATOMIC_RELEASE);` in both branches, still under caller lock/IRQ-off discipline. PCID tagging applies to CR3 only.
10. `isr_stubs.asm`: after `.load_cr3` clear (424-425), insert FS apply preserving rax/rcx/rdx + r11 (no calls, r11 read-only index): load `scheduler_load_tls_from[r11]`; if nonzero `wrmsr(MSR_FS_BASE, value)`; clear slot to 0 on both paths; if zero skip; clear the slot in `.abort_switch` beside the CR3/next-id clears (434-438).
11. aarch64 `vectors.S` (CONFIG_ARCH_AARCH64 guard): place the new `.tls_apply` block AFTER the existing `isb` (:249), before `.restore` — retarget :239 `cbz x3, .restore` → `cbz x3, .tls_apply` so the TTBR path keeps its mandatory isb on every sub-path (the S2 fix: never branch over :249 from the CR3-taken path); `.tls_apply` uses the NON-indexed pattern (base = [0], single-core exact): if nonzero `msr tpidr_el0, x-reg`; clear slot; own trailing `isb`.
12. riscv64 `syscall_entry.S` (CONFIG_ARCH_RISCV64 guard): do NOT write live tp at the satp-switch site; instead store the slot value into the trap frame `OFF_TP(sp)` save area after the satp block (:210), then clear the slot, before `7:` (:212) — using a UNIQUE numeric label (not `7:`, S3 fix), t0/t1 scratch; `sfence.vma` NOT needed for tp.
13. `TaskFields`: add `uint64_t tls_base;` after `iopb_slot` (608) + capture (4265) / restore (4353) beside iopb_slot handling. Unchanged.
14. `syscall.h`: verify-only (SYS_TLS_SET=85, SYS_MAX=86 present); add `sys_tls_set` wrapper only if absent. Unchanged.
15. Tests: as planned, PLUS `tls_self_apply_live` (setter's live FS_BASE/TPIDR/tp equals base on return), `tls_exec_clears` (exec resets TCB + live register), `tls_cancel_clears_slot` (cancel paths leave no stale TLS), `tls_second_publish_site` (tick-path dispatch publishes TLS), PLUS an aarch64-side pin for the publish+arm atomicity invariant (S3 fix — x86 cancel tests alone do not cover the vectors.S path).
