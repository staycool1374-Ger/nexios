# AUDIT REPORT 2026-09-25T00:59:25Z
PATCH: audits/pending_patch.diff
FILES: src/kernel/arch/riscv64/syscall_entry.S, src/kernel/core/global_state.cpp, src/kernel/memory/vmm.cpp, src/kernel/syscall/syscall_handlers_misc.cpp, src/kernel/syscall/syscall_handlers_process.cpp, src/kernel/task/scheduler.cpp, src/kernel/task/scheduler.hpp

## FINDINGS
- [S2] src/kernel/memory/vmm.cpp:825 — OOM unwind frees PMM pages from never-written dst_l1 slots (no zero-init after alloc).
  WHY: PMM::alloc_user_page returns recycled pages without zeroing (verified pmm.cpp:247-270), so slots at/past the break index hold stale contents and any stale PRESENT/no-RWX pattern drives free_page on a non-owned page-table page, corrupting the PMM.

## PATCH
audits/rejected_patch.diff was written (git apply --check passes); it zero-initialises the fresh dst L1 page before the copy loop so the OOM unwind scans only owned entries.

DECISION: REJECTED
