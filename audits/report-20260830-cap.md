# AUDIT REPORT 2026-08-30T10-56-32Z
PATCH: audits/pending_patch.diff
FILES: docs/specs/configuration.md, docs/specs/cspace.md, docs/specs/iommu.md, src/kernel/cap/cap_types.hpp, src/kernel/cap/iommu.hpp, src/kernel/cap/iommu.cpp, src/kernel/iommu/vtd.hpp, src/kernel/iommu/iommu.hpp, src/kernel/iommu/iommu.cpp, src/kernel/nexios_config.h, src/kernel/syscall/syscall.hpp, src/kernel/syscall/syscall_handlers_iommu.cpp, src/kernel/test/test_cap_iommu.cpp, src/kernel/test/test_expected_counts.hpp, src/kernel/test/test_registry.cpp

## FINDINGS

- [S3] src/kernel/iommu/iommu.cpp:394-395 — `clear_attachment` still unconditionally zeroes the whole-bus root entry (`g_root_table[bdf.bus]`), unbinding the shared per-bus context table for OTHER devices on the same bus (fail-closed overreach; re-attach restores). Carried over UNCHANGED from iteration 1 — not blocking per decision rule.
  WHY: availability-only imprecision with fail-closed direction — note, no action required for this cycle.
- [S3] src/kernel/iommu/iommu.cpp:384-402 — `clear_attachment` never releases the per-bus context-table slot (`g_ctx_bus_ids[i]`) even when that bus's table becomes fully empty, so after attach/clear cycles across more than `CONFIG_IOMMU_MAX_BUSES` distinct buses `attach_device` fails permanently (bounded, fail-closed). Hardening: reclaim the slot when no Present context entry remains in the bus table.
  WHY: permanent (though bounded and fail-closed) functional degradation of a newly introduced resource pool — hardening note only, no unsafe state.

Verification of iteration-1 S2 fixes (both CONFIRMED FIXED in the worktree, which matches the patch):

1. sl_walk unwind (was: orphaned linked pages on mid-walk PMM exhaustion) — src/kernel/iommu/iommu.cpp:106-142. The walk records each freshly linked page (`linked_entry[nlinked]`/`linked_page[nlinked]`, arrays sized `kSlLevels-1` = 3, matching the max 3 linkable levels — no overflow); on allocation failure (:132-135) it walks the stack deepest-first, zeroing the parent entry BEFORE freeing the page — each linked page freed exactly once, no write-after-free (the parent table of link k is link k-1's page or the root, written at iteration k before that page is freed at iteration k-1). Unwound pages are freshly allocated single-owner pages referenced only by entries this walk wrote, so no third party can observe the freed pages. Interplay with the callers is double-free-free: `map_frame`'s rollback (:293-296) runs `clear_leaf` only after the failed walk has fully unwound, and `clear_leaf` walks fresh from the root with alloc=false, aborting at the first zero entry — it can never reach or free an unwound page; pre-existing shared tables were only READ by the failed walk (`next != 0`), never linked, so the unwind cannot free them. `cascade_free_empty` frees only path levels 3..1 (never the root, which `domain_destroy` alone frees) and only after a 512-zero-entry scan plus parent-entry zeroing, so a later walk aborts at the zeroed parent — no double-free and no leak across unmap/destroy/rollback paths (:230, :296, :330).
2. bdf_valid guards (was: ctx_index OOB up to 2295 vs 256-entry tables) — `bdf_valid` at :94 requires device < 32 && function < 8 (ctx_index ≤ 255); guards present at :341 (`attach_device`), :387 (`clear_attachment`), :451 (`context_asr`) — grep confirms these are the ONLY three `ctx_index` call sites (:377, :398, :459) and each is behind its guard. `g_root_table[bdf.bus]` indexing is inherently in bounds (raw uint8_t bus ≤ 255 vs 256 entries), as is `root_entry_present(uint8_t)`.

Re-run of CRITICAL CHECKS on the whole new patch — clean: no hidden dynamic allocations (PMM SL pages + one MemPool cap object are the documented budget; `create()` rolls back the claimed domain on MemPool failure; `dispose`/`revoke` are idempotent and destroy the domain fail-closed FIRST); all 12 state-touching manager entry points take `SpinLockGuard` RAII and the leaf-lock discipline holds (only leaf PMM alloc/free inside `g_lock_`; no cap/cspace/`dispose()`/`MemPool::free` under it; syscall pin acquire/release balanced on every failure exit, no cspace+manager lock overlap); no assertion weakened and the expected-count bump 978→990 is purely additive +12 (12 new tests registered in registry/expected-counts/class table — no Heisenbug masking); `#if defined(CONFIG_ARCH_X86_64)` handlers have symmetric `#else` shapes returning -1 with `(void)`-ed params and no uninitialized variables; `force_present` deliberately not #ifdef-gated (control flow identical debug/release); syscall table compile-time sized (MAX_SYSCALL=61, entries 59/60 aligned with the enum order); `static_assert(CONFIG_IOMMU_MAX_BUSES <= 8)` keeps the `g_ctx_bus_ids` initializer exhaustive.

## PATCH

n/a — APPROVED (no corrective patch written; the two S3 notes above are hardening suggestions for a future cycle, the second S3 is carried over from iteration 1).

DECISION: APPROVED
