# AUDIT REPORT 20260921-191821
PATCH: audits/pending_patch.diff
FILES: mk/rules.mk, src/kernel/arch/aarch64/test_aarch64.cpp, src/kernel/test/test_expected_counts.hpp

## FINDINGS
- [S2] mk/rules.mk:374,377 — PRIOR S2 (report-20260921-191224) FIXED: release $(KERNEL) prerequisites + link line now include $(FORK_MARKER_OBJ), mirroring $(KERNEL_DEBUG) (rules.mk:366,369) and VERIFY_IMG_OBJ placement; application of audits/rejected_patch.diff is exact (both hunks verbatim)
  WHY: No link configuration can now emit undefined _binary_fork_marker_img_start/end refs — both kernel links consume the same provider object, and the ifeq ($(ARCH),aarch64) guard (rules.mk:343-351) expands FORK_MARKER_OBJ to empty on x86_64/riscv64 so those links are byte-identical; reachability of the aarch64 release link is irrelevant — the latent inconsistency is eliminated by construction.
- [S3] src/kernel/arch/aarch64/test_aarch64.cpp:157 — wait_for_termination_safe is unbounded (spins to TERMINATED)
  WHY: Stands as note only — suite precedent (driven EL0/user tests) with the QEMU harness timeout as the bound; unchanged from prior audit.
- [S3] src/kernel/arch/aarch64/test_aarch64.cpp:113-178 — early JARVIS_ASSERT failures (which `return`) skip teardown, leaking staged files/loaded TCB on failure paths
  WHY: Stands as note only — contained by the test boundary (mark_vfs_touched at entry drives tmpfs_reset_root + daemon reload + ResourceTracker check/restore); pre-resource assert allocates nothing.
- [S3] src/kernel/arch/aarch64/test_aarch64.cpp:144 — fd1 direct slot overwrite decs a vnode ref never inc'd on the terminate+drain path
  WHY: Stands as note only — byte-identical to the accepted test_libc_verify no-dec discipline + teardown idiom, zero-delta gated in-tree.

## VERIFIED CLEAN (no finding)
- Checks 1-5: the new mk/rules.mk hunk is a build-rule prerequisite/object-list change only — no heap allocation, no concurrency boundary, no assertion change, no PMM/BufferPool path, no critical-section interference.
- Check 6 (preprocessor/conditional): ifeq ($(ARCH),aarch64) guard defines FORK_MARKER_OBJ/_SRC/_STRIPPED vs empty else-branch symmetrically, mirroring VERIFY_IMG_OBJ (x86_64-gated); empty expansion in prerequisites and link line is safe make semantics; strip rule uses $(AARCH64_TRIPLET)strip only where the guarded chain can fire.
- Genuine marker, bounded loops (size-driven staging with written<=0 bail; capture capped at sizeof(capbuf)-1 with rd<=0 break; fm_capture_contains NUL-terminated), arch gating (#if CONFIG_ARCH_AARCH64 line 22 to EOF), counts arch_aarch64 27 single guarded row, TEMP-token sweep zero (grep clean), extern refs (start/end only) satisfied by FORK_MARKER_OBJ redefine-syms — all re-confirmed in worktree.
- Check 7: prior retrieval artifacts PRESENT on issue #104 (per report-20260921-191224); the sole delta since that audit is the auditor-directed 2-line build-rule hardening, which requires no new graphify/vault retrieval.

DECISION: APPROVED
