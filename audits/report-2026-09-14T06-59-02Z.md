# AUDIT REPORT 2026-09-14T06-59-02Z
PATCH: (none — PRE-IMPLEMENTATION DESIGN review v3, no code diff exists; third review of issue #25 Phase C1 after REJECTED report-2026-09-14T06-46-10Z and REJECTED report-2026-09-14T06-54-59Z)
FILES: docs/specs/per-cpu-smp.md (§3.4 v3, lines 95-322), src/kernel/task/scheduler.cpp, src/kernel/task/scheduler.hpp, src/kernel/task/ready_queue_manager.cpp, src/kernel/task/task.hpp, src/kernel/task/taskdefs.cpp, src/kernel/task/task.cpp, src/kernel/sync/queue.cpp, src/kernel/sync/spinlock.hpp, src/kernel/ipc/ipc.cpp, src/kernel/cap/endpoint.cpp, src/kernel/test/test_isolate.cpp, src/kernel/arch/x86_64/hal/smp.cpp, src/kernel/core/global_state.cpp, src/kernel/syscall/syscall_handlers_irq.cpp

## FINDINGS
- [S3] R1 grandfather-contradiction — CLOSED. docs/specs/per-cpu-smp.md §3.4.2 L144-155 withdraws "existing sites unchanged" and converts move_priority/enqueue/dequeue/remove to blocking global-lock with task-context-only contract (ISR callers try_lock+defer), with full order IPC/slot→scheduler→mailbox. Gap premises verified real: set_priority IrqGuard-only (scheduler.cpp:364-376), move_priority lock-free (:359-362), IPC-inheritance families IrqGuard-only (ipc.cpp:553-568,611-618; queue.cpp:539-549,572-580,600-606,625-631; endpoint.cpp:79-84). Hardening: implementation must prove task-context for the endpoint-destructor path (holds q.lock_ across the converted call, :56-88) and syscall reachable set_priority callers.
  WHY: Design text now states exactly what v2 required; residual is implementation-time proof obligation only.
- [S3] R2 snapshot AP-sync — CLOSED. v3 §3.4.4 L216-225 (atomic quiesce flag, AP tick checks first and parks) + §3.4.5 L263-274 (per-CPU currents/atoms/mailbox captured, id+magic+generation validation, mailbox clear-on-restore, AP-current-from-capture vs BSP RSP-match) covers all three v2 demands; gap premises verified (snapshot_create/restore IrqGuard-only, test_isolate.cpp:310-311,626-629; no per-CPU capture). Notes: quiesce flag memory ordering unstated (needs ACQUIRE/RELEASE like scheduler_ready); BSP-tick-vs-quiesce interaction should be stated explicitly (BSP needs no check: snapshot path masks BSP IRQs via IrqGuard, teardown path is lock-skipped); restore-drain vs id_table-restore ordering for mailbox validation should be pinned at implementation.
  WHY: Both synchronization halves plus both semantic rules are now in the text; residuals are documentation precision, not correctness holes.
- [S3] R3 scheduler_ready atomics — CLOSED. v3 §3.4.3 L181-182 specifies __atomic bool, BSP RELEASE publish, AP ACQUIRE spin + arch::pause(); flag grep-negative in-tree confirms pure-design status with nothing to contradict it.
  WHY: One-line fix fully specified as v2 required.
- [S2] docs/specs/per-cpu-smp.md §3.4.2 L156-162 vs src/kernel/task/scheduler.cpp:1242-1244,1734-1736,460-546, src/kernel/test/test_isolate.cpp:704, src/kernel/task/scheduler.cpp:1890-1913, src/kernel/sync/spinlock.hpp:43-49 — R4 STILL-OPEN: "pop/flush/drain take global then leaf" has no locked-caller discipline. on_tick already holds scheduler_lock_ (adopt_lock :1242-1244) across flush_zombies() (:1735); snapshot_restore calls drain_zombie_list() (test_isolate.cpp:704) and cleanup_test_tasks calls drain_zombie_list() (:1913) while v3 places both windows under the global lock. SpinLock is non-recursive (atomic_exchange, no owner check, spinlock.hpp:43-49), so a blocking re-take self-deadlocks deterministically on all three paths. Also undispositioned whether cleanup()+MemPool::free happen under global (reintroduces the starvation the :487-492 comment forbids) or outside it (two-phase pop-then-free, which the text never states).
  WHY: The stated order converts a race into a deterministic self-deadlock on the ISR on_tick path plus two task-context teardown paths; require locked/unlocked variants (or caller-holds protocol) and an explicit free-outside-global rule.
- [S3] R5 teardown AP-running tasks — CLOSED on the v2 demand. v3 §3.4.4 L216-225 (quiesce across teardown) + §3.4.8 L305-311 (is_idle_task() + spare-current-on-any-CPU with loud warn) covers it; gap premises verified (cleanup_test_tasks spares idle+running only, scheduler.cpp:1890-1904; reboot kill loop spares idle+self only, taskdefs.cpp:197-203).
  WHY: Both halves (quiesce + spare-any-current) are now stated with exact sites.
- [S3] R6 liveness bound — CLOSED with quantification debt. v3 §3.4.4 L207-215 gives the O(MAX_TASKS) argument plus the 1 s rendezvous fail-closed watchdog, which literally satisfies "max-hold number or watchdog"; MAX_REAP=64 and bounded collect loop verified (scheduler.cpp:1754-1760). Note for implementation: per-iteration cost under the on_tick-held global includes Logger::info serial busy-wait and TaskControlBlock::create allocation in the idle-recreate branch (:1838-1862), and the parent re-scan is O(n²)-bounded (:1795-1814) — the "no blocking syscalls/IO" sentence overstates; quantify worst-case hold or silence/hoist IO+alloc out of the locked region.
  WHY: Watchdog satisfies the v2 requirement; residual is bounding precision, not absence of an argument.
- [S2] docs/specs/per-cpu-smp.md §3.4.4 L216-225 (quiesce) vs src/kernel/task/scheduler.cpp:383-403 — NEW: quiesce parks AP dispatch but never cancels an already-armed per-CPU deferred switch. ap_tick (= rate_monotonic_schedule on own state) may publish the per-CPU arm before observing quiesce; the ISR epilogue then applies the switch after teardown has freed the target (quiesce covers no arm-cancel; cancel_pending_switch exists only as a global single-CPU primitive, :383-403, with no per-CPU variant invoked on the quiesce path).
  WHY: Publish-then-quiesce-then-apply steers the AP onto a freed stack — the exact UAF class quiesce was introduced to close, surviving in the arm window.
- [S2] docs/specs/per-cpu-smp.md §3.4.5 L245-249 (set_affinity "refuses tasks running on another CPU") vs src/kernel/task/scheduler.cpp:900-902 — NEW: the running-check is check-then-act against a lock-free writer. AP current is published by the deferred-switch ISR-epilogue apply path without scheduler_lock_ (current_task() itself is a plain unsynchronized read, :900-902), so BSP holding the global across check→dequeue→enqueue does not stabilize "running on another CPU": the AP can publish/apply a switch onto the task mid-window, and re-queueing an AP-RUNNING task inserts a RUNNING TCB into the runq (INV-2 wedge class).
  WHY: Global-lock coverage cannot fence a lock-free current update; the refusal check needs a protocol that covers the apply side (re-validate after requeue, or quiesce-scoped affinity changes).
- [S3] Design-precision notes, no correctness hole: (a) TCB generation is uint32_t monotonic never reset (task.hpp:543; task.cpp:276-280; reset_next_task_id resets IDs only, scheduler.cpp:1106-1107) — mailbox id+generation validation is sound short of 2^32 creations; state the wrap assumption. (b) Blocking global take in snapshot_create while AP holds via rms is deadlock-free (AP try-only, BSP IF=0-masked so no self-ISR) and bounded by the rms section — covered by the R6 quantification note. (c) set_affinity dequeue+enqueue under the global correctly excludes AP rms dispatch mid-window; only the running-check (above) escapes it.
  WHY: Implementation constraints the plan should pin down, not rejection-grade defects.

## PATCH
No machine-applicable patch: pre-implementation design review with no code diff. Required design changes for resubmission: (1) fix R4 zombie nesting — locked/unlocked variants or caller-holds protocol for flush/drain/cleanup_step across the three already-locked callers (on_tick, snapshot_restore, cleanup_test_tasks) plus an explicit free-outside-global two-phase rule; (2) quiesce must cancel/inhibit the AP's armed per-CPU deferred switch (per-CPU cancel or arm-drain under quiesce) with stated flag ordering; (3) fence set_affinity's running-on-another-CPU check against the lock-free AP current-apply path.

DECISION: REJECTED
